# 판매가 자동화 시스템 기획서

## 1. 현재 워크플로우 분석

### 1.1 일일 작업 순서

**[RAW DATA 단계] — 매일 아침 수동 작업**

1. 천년경영 프로그램에서 엑셀 다운로드 (상품관리: 기존/변경 매입가, 당월매출현황, 신선행당월매출현황, 8일간 매입현황, 가락시장 경매가)
2. RAW DATA 구글시트에 데이터 붙여넣기
3. 자동화 메뉴 실행 → ★ 플랫폼 시트로 데이터 전송
   - 시트 복제: 기존, 변경, 주차별, 식봄리스트, 식봄매출, 온일장매출, 경매가, 신선행매출
   - 월별 통합 매출 데이터 생성 (전체 + 신선행)
   - 상품별 7일 최고가/중앙값/가중평균 계산
   - 일주일 동향 분석 (절사평균 기반 5단계 판정)

**[★ 플랫폼 단계] — 판매가 결정 및 업로드**

4. **최근7일 매입가 노트 삽입 실행** (가장 먼저 — 이후 판단의 기초 데이터)
5. 전체상품(야채용) 시트에서 어제 판매가를 "기존판매가"에 복사 붙여넣기
6. 수익률 19.5% 미만 상품 → "수익률일괄변경시 가격"으로 먼저 일괄 교체
7. 상품그룹별 매입가 변동 확인 및 수동 판매가 조정 (핵심 판단 업무)
8. 판매가변경상품(야채용) 추출 실행
9. 통합 업로드 실행: 식봄 → 온일장 → 신선행 → 배민 각 플랫폼별 업로드 파일 생성

### 1.2 현재 시스템 구성

| 시스템 | 스프레드시트 ID | 역할 |
|--------|----------------|------|
| RAW DATA | `1PI__mbQW6NFRW0CnhLKkTVa25q2DX7Wibpycj2tjj7Y` | 천년 데이터 수집 + 분석 가공 |
| ★ 플랫폼 | `1XTEsLofvN7c3VVg110OYkOoSnttAgPF9_JYxr4cGkfE` | 판매가 관리 + 플랫폼별 업로드 |
| 신선행 | `1vAJYbhec7Hp-F7bkCpVq1kw0vEwbiyq6yqGJn5NDSRg` | 신선행 전용 |

---

## 2. 판매가 변경 기준 규칙

### 2.1 수식 기반 자동 계산 규칙 (현재 구글시트에서 작동 중)

#### 기본 수식 체계

| 항목 | 수식 | 설명 |
|------|------|------|
| **수익률** | `1 - (매입가 ÷ 판매가)` | 식봄 기준 |
| **신선행판매가** | `MAX(ROUNDUP(식봄판매가 × 0.94, -1), ROUNDUP(매입가 ÷ 0.9, -1))` | 식봄가의 6% 할인가 vs 매입가 기준 최저수익률 10% 보장가 중 큰 값 |
| **배민판매가** | `= 식봄판매가` | 동일 적용 |
| **수익률일괄변경시 가격** | `ROUNDUP(매입가 ÷ (1 - 목표수익률), -1)` | 상품별 수동 지정 목표수익률 기준 |
| **변동률** | `변동액 ÷ 변경매입가` | 기존 대비 변경 매입가 비율 |
| **변동액** | `변경매입가 - 기존매입가` | |

#### 신선행판매가 상세 로직
- 식봄판매가 × 0.94 = 식봄 대비 6% 할인 (식봄 수익률 20%일 때 신선행 약 14.89%)
- 매입가 ÷ 0.9 = 최저 수익률 10% 하한선
- 두 값 중 큰 값 적용 → 수익률 10% 이하 방지
- 단, 식봄 수익률이 11.1% 미만이면 두 값이 역전됨 (하한선이 더 커짐)

#### 일주일 동향 분석 로직
- 8일간 매입가 중 1일차를 기준값으로 설정
- 2~8일차 중 최대값, 최소값 각 1개씩 제외 (절사평균)
- 변동률 기준 5단계 판정:
  - +20% 초과: **급등** / +3%~+20%: **상승** / ±3%: **보합** / -3%~-20%: **하락** / -20% 초과: **급락**
- 매입가 일별 데이터 4일 미만 시 계산 제외

#### 직전3개월 대비 당월 판매량 분석
- 직전 3개월 중 매출 있는 월만 계산 (미매출 기간 제외)
- 각 월의 일평균 판매량 산출 → 평균 → 당월 일수 기준 기대치 계산
- 당월 실제 판매량과 비교하여 등락률 표시
- 당월 매출만 있을 시 "신규매출" 표시
- 플랫폼 합계 기준 (신선행 매출은 괄호 안에 별도 표기)

### 2.2 판단 기반 수동 조정 규칙 (자동화 시 로직으로 변환 필요)

#### 규칙 1: 아침 긴급 일괄 조정
- **조건**: 수익률 19.5% 미만인 상품
- **조치**: 수익률일괄변경시 가격을 판매가로 일괄 적용
- **목적**: 마진이 너무 낮아진 상품을 빠르게 정상화

#### 규칙 2: 상품그룹 내 가격 동조화
- **원칙**: 같은 상품그룹(예: 가지 그룹) 내에서 매입이 있는 상품의 가격 추세로 매입이 없는 상품도 동일하게 조정
- **왜 필요한가**: 모든 야채가 매일 매입되지 않는다. 가지/상은 매입이력이 없는 날에도 판매는 계속된다. 매입이 있는 가지/특의 가격이 급등했는데 가지/상의 판매가를 그대로 두면, 역마진을 본 후에야 가격을 수정하는 상황이 발생한다

- **대표상품(기준상품) 선정 로직**:
  - 같은 상품그룹 내에서 **매입 빈도가 가장 높은 상품**이 대표상품이 된다
  - 예: 가지 그룹에서 가지1kg(247건), 가지/상(291건), 가지/특(284건) → 가지/상이 대표
  - 만약 모든 가지 상품 중 가지3개만 매일 매입이 있었다면, 가지3개를 기준으로 매입가 변동 흐름을 판단
  - 그룹 내 어느 하나라도 변동률이 크면, 나머지 상품도 변동 여부를 체크해야 함

- **소분상품 구조** (2025년 데이터 기준):
  - 가지/특(003362, 박스) → 소분 → 가지3개(013899), 가지5개(013900), 가지10개(013901)
  - 소분상품은 매입이력이 없고 매출만 존재 (원물 매입 후 소분하여 판매)
  - 소분상품의 판매가는 원물(가지/특)의 매입가 변동에 따라 조정해야 함
  - 같은 패턴: 청오이→3/5/10개, 백오이→3/5/10개, 팽이버섯→3/5/10봉, 애호박→3/5/10개

- **변동률 해석 주의사항**:
  - 가지3개는 변동률 20%인데 가지5개는 변동률 0% → 이유: 가지3개는 며칠 건너뛰고 매입이력이 있어서 간격 사이에 가격이 변함, 가지5개는 매일 매입이 있어 어제와 동일 가격
  - 동일 상품의 모든 등급이 똑같은 등락을 보이진 않지만, **매입이 빈번한 상품을 기준으로** 그 상품의 등락을 판단

- **판단 기준 — 두 가지 관점을 종합 판단**:
  - **동향 관점 (절사평균 기반)**: 일주일간동향 분석값. 8일간 매입가에서 최고값·최저값을 제외한 절사평균으로 산출하므로, 극단적인 1~2일 급등락이 걸러져 "전체적인 흐름"을 보여준다. 이 기준으로는 보합으로 판정될 수 있다
  - **실매입가 관점 (노트 팝업)**: 최근7일 매입가 노트에 표시되는 실제 매입이 일어난 날의 매입가. 매입이 없는 날은 빈칸이므로, 있는 날의 가격만 직접 비교하면 하락으로 보일 수 있다
  - **핵심**: 같은 상품이라도 동향=보합, 실매입가=하락처럼 두 관점이 다를 수 있다. 판매가 조정 시 한쪽만 보지 않고 두 관점을 함께 보고 판단한다

#### 규칙 3: 7일간 가격 흐름 기반 조정
- **안정적 흐름 (보합 또는 일관된 상승/하락)**: 수익률일괄변경용 수익률 기준으로 판매가 책정
- **불규칙한 흐름 (급등락 반복)**: 7일 최고가에 가까운 가격 또는 수익률 15% 미만으로 보수적 설정
- **이유**: 가격이 불안정할 때 높은 매입가에 맞춰 판매가를 설정해야 손실 방지

#### 규칙 4: 판매량 기반 수익률 단계 조정
- **판매량 0~9개**: 기본 수익률 15%부터 시작
- **판매량 10~19개**: 수익률 16%대로 상향
- **판매량 20개 이상**: 수익률 17~18%대로 상향
- **소분 단위 (봉, 개, 1kg 미만)**: 박스 단위보다 수익률을 높게 설정
- **변수**: 가격 급등 시 수익률 하향 가능, 가격 폭락 시 마진 추가 확보

#### 규칙 5: 주요 품목 경쟁력 유지
- **대상**: 무, 대파, 양배추, 쪽파, 양파, 양상추, 미나리, 버섯, 알배기 (웹에서 목록 수정 가능)
- **원칙**: 10명의 판매자 중 가장 저렴한 축에 속하도록 유지
- **이유**: "이 업체는 항상 저렴하다"는 인상 = 매출을 일으키는 핵심 품목
- **수익률**: 경쟁 치열하여 15% 미만도 가능
- **가격 테스트**: 간헐적으로 수익률을 올려보고 판매량 반응을 관찰

#### 규칙 6: 시장 상황 반영
- 가격이 꾸준히/갑자기 치솟을 경우: 저렴할 때와 같은 수익률 유지 불가 → 수익률 20% 미만으로도 운영
- 가격 폭락 시: 마진을 살짝 더 확보
- 날씨, 이슈, 계절 요인도 판단에 반영 (현재 수동)

#### 규칙 7: 행사품목 처리
- 행사품목 태그가 있는 상품: 판매가 × 1.25 해서 정상가 계산 (25% 할인 표시용)
- 식봄 업로드 시 정상가와 행사가가 다르면 할인 표시

#### 규칙 8: 매입가 하락추세 + 매출 둔화 시 점유율 확대 판단
- **상황**: 매입가가 2~3일 연속 하락 + 직전3개월평균 매출 상승률이 급격히 둔화 (예: 25%→5%)
- **판단 순서**:
  1. 8일간 매입가를 확인하여 급락(일시적)인지 하락추세(지속적)인지 구분
  2. 하락추세가 2~3일 지속 확인 시 → 판매가를 내려서 점유율 확대 모드로 전환
  3. 목표수익률을 기존값(예: 21.2%)에서 낮춰서 19~20%대로 설정
- **기준매입가 선정 방법 (변곡점 방식)**:
  - 예시 매입가: 5000, 5200, 5400, 5000, 5300, **4600**, 4350, 4250
  - 앞구간(5000원대 박스권)에서 벗어나는 첫 지점 = **4600원이 기준매입가**
  - 최저값(4250)을 쓰면 너무 공격적, 8일 평균(4700)을 쓰면 너무 둔함
  - 변곡점(이전 박스권 이탈 시점)이 현실적인 기준
- **판매가 산출**: 기준매입가 4600원 × (1 ÷ (1 - 0.19)) ≒ 5,680원 → ROUNDUP(-1) → 5,680원
- **핵심 인사이트**: 목표수익률은 고정값이 아니라, 시장 상태에 따라 달라지는 **정책 변수**. "21.2%를 유지할까?"가 아니라 "지금이 마진 방어 국면인가, 점유율 회복 국면인가?"를 먼저 판단

### 2.3 플랫폼별 판매가 관계

```
식봄 판매가 (기준)
├── 배민 판매가 = 식봄 판매가 (동일)
├── 신선행 판매가 = MAX(식봄 × 0.94, 매입가 ÷ 0.9) (6% 할인, 최저수익률 10%)
└── 온일장 판매가 = 식봄 판매가 기반 (업로드 시 N,O,S,T열 동기화)
```

### 2.4 API 연동 현황

| 플랫폼 | API | 현재 방식 |
|--------|-----|-----------|
| 식봄 | 미제공 | 쿠키 기반 UrlFetch로 엑셀 다운로드/업로드 |
| 온일장 | 미제공 | 엑셀 수동 업로드 |
| 배민상회 | 미제공 | 엑셀 수동 업로드 |
| 신선행 (자사) | **가능** | API 연동 가능 |

---

## 3. 구글시트 데이터 한계 분석

### 3.1 현재 데이터 규모

| 시트 | 탭 수 | 주요 시트 크기 | 추정 셀 수 |
|------|--------|----------------|------------|
| RAW DATA | 11개 | 기존(0325): 49열×8,961행 | ~200만 셀 |
| ★ 플랫폼 | **53개** | 전체상품: 45열×2,021행 / 야채용: 58열×731행 | ~300만 셀 |

### 3.2 문제점
- 구글시트 제한: 스프레드시트당 **1,000만 셀**
- ★ 플랫폼 시트에 **월별 성장/첫구매 시트가 20개 이상** 계속 추가 중
- 매월 새 시트가 생기면서 점점 무거워짐
- Apps Script 실행 시간 제한 (6분) 접근 중

### 3.3 즉시 가능한 정리
- 과거 월별 성장/첫구매 시트(12월, 1월, 2월 등) → 별도 아카이브 스프레드시트로 이전
- 시트 수 53개 → 약 30개 이하로 축소 가능

---

## 4. 자동화 아키텍처 설계

### 4.1 기술 스택

| 구성요소 | 기술 | 역할 |
|----------|------|------|
| 데이터베이스 | **Supabase** (PostgreSQL) | 구글시트 대체, 모든 데이터 저장 |
| 프론트엔드 | **Next.js** (Vercel 배포) | 판매가 관리 웹 대시보드 |
| 백엔드 API | **Vercel Functions** 또는 **Railway** | 데이터 처리, 분석 엔진, 파일 생성 |
| 코드 관리 | **GitHub** | 버전 관리 |
| AI 보조 | **Cowork (Claude)** | 개발 지원, 판매가 추천 로직 고도화 |

### 4.2 전체 흐름

```
[천년경영 프로그램]
    │ 수동 엑셀 다운로드
    ▼
[웹 대시보드 - 데이터 업로드]
    │ 드래그앤드롭으로 엑셀 업로드
    ▼
[Supabase DB 저장]
    │ 자동 파싱 + 저장
    ▼
[분석 엔진 자동 실행]
    ├── 매입가 변동률/변동액 계산
    ├── 7일 동향 (절사평균 5단계)
    ├── 상품별7일 최고가/중앙값/가중평균
    ├── 월별 판매량 추이 분석
    ├── 상품그룹 내 가격 동조화 추천
    └── 추천 판매가 산출 (규칙 기반)
    ▼
[웹 대시보드 - 판매가 조정]
    ├── 추천판매가 + 근거 표시
    ├── 이상치 하이라이트 (급등/급락)
    ├── 수동 수정 가능
    └── 일괄 확정 버튼
    ▼
[플랫폼별 업로드 파일 자동 생성]
    ├── 식봄 업로드용 (엑셀)
    ├── 온일장 업로드용 (엑셀)
    ├── 배민 업로드용 (엑셀)
    └── 신선행 API 직접 연동
```

### 4.3 Supabase DB 테이블 설계 (초안)

```sql
-- 상품 마스터
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) UNIQUE NOT NULL,  -- 천년 상품코드
  category_name VARCHAR(100),               -- 소분류명
  product_group VARCHAR(10),                -- 상품그룹 코드
  product_name VARCHAR(200),                -- 상품명
  spec VARCHAR(100),                        -- 규격
  unit_type VARCHAR(20),                    -- 단위 (박스/봉/개/kg 등)
  is_event_item BOOLEAN DEFAULT FALSE,      -- 행사품목 여부
  target_margin_rate DECIMAL(5,2),          -- 목표수익률 (수익률일괄변경용)
  is_key_item BOOLEAN DEFAULT FALSE,        -- 주요 경쟁 품목 여부
  platform_codes JSONB,                     -- 각 플랫폼별 상품코드 매핑
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 일별 매입가
CREATE TABLE daily_purchase_prices (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) REFERENCES products(product_code),
  price_date DATE NOT NULL,
  purchase_price INTEGER,                   -- 매입가 (천년 상품관리 기준)
  actual_purchase_price INTEGER,            -- 실매입가 (매입현황 기준)
  auction_price INTEGER,                    -- 가락시장 경매가
  quantity DECIMAL(10,2),                   -- 매입수량
  UNIQUE(product_code, price_date)
);

-- 일별 판매가 (확정 기록)
CREATE TABLE daily_selling_prices (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) REFERENCES products(product_code),
  price_date DATE NOT NULL,
  sikbom_price INTEGER,                     -- 식봄 판매가 (기준)
  sinsunhang_price INTEGER,                 -- 신선행 판매가
  baemin_price INTEGER,                     -- 배민 판매가
  oniljang_price INTEGER,                   -- 온일장 판매가
  margin_rate DECIMAL(5,2),                 -- 수익률
  sinsunhang_margin_rate DECIMAL(5,2),
  baemin_margin_rate DECIMAL(5,2),
  adjustment_reason TEXT,                   -- 조정 사유 (선택)
  confirmed_at TIMESTAMPTZ,                 -- 확정 시간
  UNIQUE(product_code, price_date)
);

-- 월별 판매량
CREATE TABLE monthly_sales (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) REFERENCES products(product_code),
  sale_month DATE NOT NULL,                 -- 해당 월 1일
  total_quantity INTEGER,                   -- 플랫폼 전체 판매량
  sinsunhang_quantity INTEGER,              -- 신선행 판매량
  UNIQUE(product_code, sale_month)
);

-- 분석 결과 (매일 자동 계산)
CREATE TABLE daily_analysis (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) REFERENCES products(product_code),
  analysis_date DATE NOT NULL,
  price_change_rate DECIMAL(8,4),           -- 매입가 변동률
  price_change_amount INTEGER,              -- 매입가 변동액
  week_max_price INTEGER,                   -- 7일 최고가
  week_median_price INTEGER,                -- 7일 중앙값
  week_weighted_avg INTEGER,                -- 7일 가중평균
  week_trend VARCHAR(10),                   -- 일주일 동향 (급등/상승/보합/하락/급락)
  sales_trend VARCHAR(20),                  -- 판매량 추이 (▲xx%/▼xx%/신규매출)
  recommended_price INTEGER,                -- 추천 판매가
  recommendation_reason TEXT,               -- 추천 사유
  UNIQUE(product_code, analysis_date)
);
```

### 4.4 단계별 개발 계획

**1단계: Supabase DB + 데이터 업로드 (1~2주)**
- DB 테이블 생성
- 엑셀 업로드 → DB 저장 API
- 기존 구글시트 데이터 마이그레이션

**2단계: 분석 엔진 (1주)**
- 매입가 변동 계산
- 7일 동향 절사평균 분석
- 판매량 추이 분석
- 추천 판매가 산출 (규칙 2.2 기반)

**3단계: 웹 대시보드 - 조회/수정 (2주)**
- 전체상품(야채용) 화면 재현
- 상품그룹별 정렬/필터
- 판매가 인라인 수정
- 추천가 표시 + 확정 버튼

**4단계: 플랫폼별 업로드 파일 생성 (1주)**
- 식봄/온일장/배민 엑셀 파일 자동 생성
- 신선행 API 직접 연동
- 다운로드 버튼

**5단계: 고도화 (지속)**
- 경쟁사 가격 모니터링 (가능한 범위)
- 판매가 이력 대시보드
- 추천 엔진 정확도 개선

---

## 5. 3단계 하네스 프로토콜 (기획자 → 생성자 → 평가자)

### 5.1 왜 하네스 프로토콜이 필요한가

이 프로젝트는 한 번에 "구글시트 → 웹 시스템" 전환을 완성하기 어렵다. 구글시트의 수식, Apps Script 자동화, 매일 아침의 판단 업무를 단계적으로 옮겨야 하기 때문이다. 이때 "Claude에게 바로 코드를 짜 달라"고 하면 방향이 틀어지거나, 빠진 규칙을 뒤늦게 발견하거나, 결과물이 실무에 맞지 않는 문제가 생긴다.

하네스 프로토콜은 이 문제를 막는 **작업장 설계**다. 마스터 가이드의 표현을 빌리면, "좋은 프롬프트는 시작점이 되고, 좋은 문맥은 방향을 잡아 주며, 좋은 하네스는 끝까지 흔들리지 않게 붙잡아 준다."

핵심 아이디어는 GAN(적대적 생성 신경망)에서 왔다. GAN에서 생성자가 가짜 이미지를 만들고 판별자가 진위를 가리듯이, 우리도 **생성자(Claude)가 코드를 만들고 평가자(Claude 또는 형민님)가 검증하는 구조**를 반복한다. 여기에 **기획자**가 매 스프린트 시작 전에 "무엇을 만들고, 어떻게 검증할 것인가"를 정하는 스프린트 계약서를 작성한다.

### 5.2 세 역할의 정의

```
┌─────────────────────────────────────────────────────────┐
│                    스프린트 시작                           │
│                        │                                 │
│                 ┌──────▼──────┐                          │
│                 │   기획자     │  스프린트 계약서 작성       │
│                 │  (Planner)  │  → 무엇을, 어떤 기준으로   │
│                 └──────┬──────┘                          │
│                        │ 계약서 확정 (형민님 승인)          │
│                 ┌──────▼──────┐                          │
│                 │   생성자     │  코드 작성 + 구현          │
│                 │ (Generator) │  → 계약서 범위 내에서만     │
│                 └──────┬──────┘                          │
│                        │ 구현 완료                        │
│                 ┌──────▼──────┐                          │
│                 │   평가자     │  검증 + 판정              │
│                 │ (Evaluator) │  → 계약서 기준으로 합격/불합격│
│                 └──────┬──────┘                          │
│                        │                                 │
│              ┌─────────┼─────────┐                       │
│              ▼                   ▼                       │
│          [합격]              [불합격]                      │
│       다음 스프린트로         피드백 → 생성자 재작업         │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

#### 기획자 (Planner) — "무엇을 만들 것인가"
- **누가**: Claude + 형민님이 함께 작성
- **산출물**: 스프린트 계약서 (`sprint-XX-contract.md`)
- **내용**: 이번 스프린트의 목표, 구현 범위, 제외 범위, 성공 기준(합격 조건), 참조할 기존 코드/수식
- **핵심 규칙**: 계약서에 없는 것은 만들지 않는다

#### 생성자 (Generator) — "실제로 만든다"
- **누가**: Claude (Cowork)가 코드 작성
- **입력**: 스프린트 계약서 + 기존 기획서 + 참조 코드
- **산출물**: 작동하는 코드, DB 스키마, API 등
- **핵심 규칙**: 계약서의 범위를 넘지 않는다. 판단이 필요하면 형민님에게 질문한다

#### 평가자 (Evaluator) — "제대로 만들었는가"
- **누가**: Claude (별도 검증 세션) + 형민님 (실무 확인)
- **입력**: 생성자의 산출물 + 스프린트 계약서의 합격 조건
- **산출물**: 평가 보고서 (`sprint-XX-review.md`) — 합격/불합격/수정요청
- **핵심 규칙**: 계약서의 합격 조건만으로 판단한다. "잘 만든 것 같다"가 아니라 "조건 3개 중 3개 통과"처럼 정량적으로

### 5.3 스프린트 계약서 양식

각 스프린트 시작 전에 아래 양식을 채워서 `sprint-XX-contract.md` 파일로 저장한다.

```markdown
# 스프린트 [번호]: [제목]

## 목표
이번 스프린트에서 달성할 것을 한 문장으로.

## 구현 범위
- [ ] 구체적인 작업 항목 1
- [ ] 구체적인 작업 항목 2
- [ ] 구체적인 작업 항목 3

## 제외 범위
이번 스프린트에서 하지 않을 것 (범위 넘침 방지).

## 합격 조건 (Acceptance Criteria)
1. [조건 1]: 예) "엑셀 파일을 업로드하면 products 테이블에 행이 생긴다"
2. [조건 2]: 예) "매입가 변동률이 구글시트와 ±0.1% 이내로 일치한다"
3. [조건 3]: 예) "브라우저에서 /upload 페이지가 열린다"

## 참조 자료
- 기획서 섹션: 2.1, 4.3
- 기존 코드: runSyncFast() 함수의 XX 로직
- 구글시트 수식: =ROUNDUP(Z2/(1-AR2),-1)

## 검증 방법
- 자동 검증: SQL 쿼리로 행 수 확인, API 응답 코드 확인
- 수동 검증: 형민님이 실제 엑셀 파일로 업로드 테스트
```

### 5.4 개발 단계별 스프린트 계획

기존 4.4의 5단계를 스프린트 단위로 더 잘게 쪼갠다. 각 스프린트는 **1~3일** 분량이다.

#### 1단계: 기반 구축 (Supabase + 프로젝트 세팅)

| 스프린트 | 제목 | 핵심 합격 조건 |
|----------|------|----------------|
| S01 | Supabase 프로젝트 + DB 테이블 생성 | 5개 테이블이 Supabase에 존재하고, 테스트 데이터 INSERT 성공 |
| S02 | Next.js 프로젝트 생성 + GitHub 연결 | localhost:3000에서 빈 대시보드 페이지가 열림. GitHub에 push 완료 |
| S03 | 엑셀 업로드 API (천년경영 → DB) | 천년 엑셀 파일 드래그앤드롭 → products + daily_purchase_prices에 데이터 저장 |
| S04 | 기존 데이터 마이그레이션 도구 | 구글시트 CSV 내보내기 → DB 일괄 import. 상품 700개+ 이전 확인 |

#### 2단계: 분석 엔진

| 스프린트 | 제목 | 핵심 합격 조건 |
|----------|------|----------------|
| S05 | 매입가 변동 + 수익률 계산 | 구글시트의 변동률/변동액/수익률과 ±0.1% 이내 일치 |
| S06 | 7일 동향 절사평균 분석 | 5단계 판정(급등~급락)이 구글시트 결과와 100% 일치 |
| S07 | 판매량 추이 + 직전3개월 분석 | 신규매출/▲▼% 판정이 구글시트와 일치 |
| S08 | 추천 판매가 산출 (규칙 1~7) | 19.5% 미만 일괄조정 결과가 구글시트와 일치. 추천 사유 텍스트 표시 |

#### 3단계: 웹 대시보드

| 스프린트 | 제목 | 핵심 합격 조건 |
|----------|------|----------------|
| S09 | 전체상품 테이블 뷰 (읽기 전용) | 야채용 731행이 브라우저 테이블에 표시. 소분류별 그룹핑 |
| S10 | 판매가 인라인 수정 + 확정 | 셀 클릭 → 수정 → 확정 버튼 → DB 저장. 수익률 자동 재계산 |
| S11 | 추천가 표시 + 이상치 하이라이트 | 추천가 열 표시, 급등/급락 빨간색, 수익률 19.5% 미만 노란색 |
| S12 | 상품그룹 동조화 보조 UI | 같은 그룹 상품 묶어서 보기, 일괄 조정 버튼 |

#### 4단계: 플랫폼 업로드

| 스프린트 | 제목 | 핵심 합격 조건 |
|----------|------|----------------|
| S13 | 식봄 업로드 엑셀 생성 | 생성된 엑셀이 기존 Apps Script 결과물과 컬럼/데이터 일치 |
| S14 | 온일장 + 배민 업로드 엑셀 | 각 플랫폼 양식에 맞는 엑셀 생성. 기존 결과물과 일치 |
| S15 | 신선행 API 연동 | API 호출로 판매가 전송 성공. 응답 코드 200 |
| S16 | 통합 업로드 + 다운로드 UI | 한 화면에서 4개 플랫폼 업로드 파일 생성/다운로드 |

### 5.5 하네스 파일 구조

프로젝트 폴더에 아래 파일들을 유지하며 3단계 프로토콜을 운영한다.

```
판매가변경영상/
├── 판매가_자동화_기획서.md          ← 전체 설계 문서 (이 파일)
├── CLAUDE.md                       ← 프로젝트 규칙 (생성자가 반드시 읽는 계약)
├── sprints/
│   ├── sprint-01-contract.md       ← 기획자 산출물
│   ├── sprint-01-review.md         ← 평가자 산출물
│   ├── sprint-02-contract.md
│   ├── sprint-02-review.md
│   └── ...
├── handoff.md                      ← 세션 종료 시 상태 기록 (다음 세션 이어받기용)
└── src/                            ← 생성자가 만드는 실제 코드
    ├── app/                        ← Next.js 페이지
    ├── lib/                        ← 분석 엔진, DB 유틸리티
    └── ...
```

### 5.6 CLAUDE.md 초안 (프로젝트 규칙)

개발 시작 시 아래 내용을 `CLAUDE.md`에 넣는다. 생성자(Claude)는 매 세션 시작 시 이 파일을 먼저 읽게 된다.

```markdown
# 판매가 자동화 프로젝트 규칙

## 기본 원칙
- 현재 스프린트 계약서(sprints/sprint-XX-contract.md)의 범위만 작업한다
- 판매가 계산 수식은 기획서 섹션 2.1의 수식과 정확히 일치해야 한다
- 판단 기반 규칙(섹션 2.2)은 "추천"으로만 표시하고, 자동 확정하지 않는다
- 형민님이 확정 버튼을 누르기 전까지 판매가는 변경되지 않는다

## 기술 규칙
- DB: Supabase (PostgreSQL). 테이블 스키마는 기획서 4.3 참조
- 프론트: Next.js (App Router). TypeScript 사용
- 스타일: Tailwind CSS
- 배포: Vercel
- 금액은 모두 정수(원 단위), ROUNDUP(-1)은 10원 단위 올림

## 검증 규칙
- 수식 관련 변경 시: 구글시트 결과와 비교 테스트 필수
- API 변경 시: 응답 코드와 데이터 형식 확인
- UI 변경 시: 브라우저에서 스크린샷 확인

## 하지 말 것
- .env 파일 직접 수정 금지
- 판매가를 자동으로 플랫폼에 전송하지 않기 (반드시 형민님 확인 후)
- 스프린트 계약서에 없는 기능 추가하지 않기
```

### 5.7 실제 작업 흐름 (매 스프린트 반복)

**1단계 — 기획자 단계 (5~15분)**

형민님과 Claude가 함께 다음 스프린트 계약서를 작성한다.
1. 이전 스프린트 평가 결과 확인
2. 다음으로 할 작업을 기획서에서 확인
3. 계약서 양식에 맞춰 목표, 범위, 합격 조건 작성
4. 형민님이 "이대로 진행" 승인

**2단계 — 생성자 단계 (30분~2시간)**

Claude가 계약서에 따라 코드를 작성한다.
1. CLAUDE.md + 계약서 + 기획서 읽기
2. 코드 작성 (DB 스키마, API, 페이지 등)
3. 자체 테스트 실행
4. 완료 후 handoff.md 업데이트

**3단계 — 평가자 단계 (10~30분)**

계약서의 합격 조건을 하나씩 검증한다.
1. 자동 검증: SQL 쿼리, API 호출, 수식 비교
2. 수동 검증: 형민님이 실제 데이터로 테스트
3. 평가 보고서 작성 (`sprint-XX-review.md`)
4. 합격 → 다음 스프린트 / 불합격 → 수정 사항 정리 후 생성자 재작업

### 5.8 첫 번째 스프린트(S01)부터 시작하는 방법

1. 형민님이 Supabase 프로젝트를 생성한다 (supabase.com에서 무료 계정)
2. Claude와 함께 S01 계약서를 작성한다
3. Claude가 Supabase MCP를 통해 테이블을 생성한다
4. 테스트 데이터 INSERT로 합격 조건을 검증한다

시작 준비가 되면 "S01 시작하자"라고 말씀해 주시면 됩니다.

---

## 7. 추세 기반 자동판매가 의사결정 엔진

### 7.1 최종 비전

이 프로젝트의 최종 목표는 단순한 구글시트 웹 이전이 아니라 **야채판매가 자동책정시스템**이다. 핵심은 "내일 매입가를 예측하는 AI"가 아니라, **상황별로 일관된 대응을 하는 의사결정 엔진**을 만드는 것이다.

형민님이 매일 아침 하는 판단은 이미 하나의 알고리즘이다:
1. 가격이 떨어졌나? → 일시적 급락인가, 추세적 하락인가?
2. 판매도 같이 약해졌나? → 점유율이 밀리고 있는가?
3. 그럼 마진을 낮춰서라도 회전율을 올릴까? → 어느 정도까지?

이 판단을 코드로 옮기되, 첫 버전은 반드시 **추천가 제안 + 사유 설명 + 사람 승인** 구조로 간다.

### 7.2 3층 엔진 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                  A. 신호 해석층 (Signal Layer)                │
│  "지금 시장이 어떤 상태인가?"                                 │
│                                                              │
│  입력:                        산출:                           │
│  · 최근 8일 매입가            · 시장 상태 판정               │
│  · 최근 3일 기울기              (급락/하락추세/보합/          │
│  · 7일 변동성                    상승추세/급등)              │
│  · 당일 매입가 vs 기준가 괴리  · 변곡점 매입가               │
│  · 직전3개월 평균 매출 추이    · 추세 지속 일수              │
│  · 최근 7일 판매량             · 변동성 수준                 │
│  · 절사평균 동향                                             │
├─────────────────────────────────────────────────────────────┤
│                  B. 전략 선택층 (Strategy Layer)              │
│  "지금은 어떤 정책을 써야 하는가?"                            │
│                                                              │
│  시장 상태 + 매출 추이 조합 → 점수화 → 정책 선택             │
│                                                              │
│  · 마진 방어 모드: 수익률 유지/소폭 상향                     │
│  · 현상 유지 모드: 기존 수익률 유지                          │
│  · 점유율 확대 모드: 수익률 하향 (-1~2%p)                    │
│  · 공격적 인하 모드: 수익률 대폭 하향 (-2~3%p)              │
├─────────────────────────────────────────────────────────────┤
│                  C. 가격 산출층 (Price Layer)                 │
│  "구체적으로 얼마로 설정할 것인가?"                           │
│                                                              │
│  · 기준매입가 선정 (변곡점 방식)                             │
│  · 추천 목표수익률 = 기본수익률 ± 조정치                     │
│  · 최소마진 하한선 절대 유지                                 │
│  · 10원 단위 올림                                           │
│  · 플랫폼별 가격 연동 (식봄→배민/신선행/온일장)              │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
  ┌──────────────────┐
  │  추천가 + 사유    │ ← 형민님이 승인/수정 후 확정
  │  + 근거 데이터    │
  └──────────────────┘
```

### 7.3 신호 해석층 — 주식 알고리즘에서 빌려올 개념

형민님의 판단 방식은 주식 트레이딩의 사고 구조와 닮아 있다. 다만 주식 지표를 그대로 쓰는 것이 아니라, **사고방식만 빌려온다**.

| 형민님의 판단 | 주식 알고리즘 대응 개념 | 야채 시스템 적용 |
|--------------|------------------------|-----------------|
| "앞 5일은 5000원대, 뒤 3일은 4600→4350→4250" | 지지선 이탈, 레짐 전환 | **박스권 이탈 탐지**: 이전 N일 평균 대비 최근 3일이 유의미하게 벗어남 |
| "2~3일 연속 하락인지 확인" | 추세 지속성 확인 | **추세 지속 일수**: 같은 방향 연속일 카운트 |
| "4600원을 기준매입가로" | 변곡점 탐지 | **변곡점 가격**: 이전 박스권과 신규 추세의 경계 |
| "하락폭이 커지는지 확인" | 모멘텀 가속/감속 | **기울기 변화율**: 최근 3일 하락폭이 확대 vs 축소 |
| "변동성 큰 상품은 함부로 안 건드림" | 변동성 기반 포지션 사이징 | **변동성 계수**: 고변동 품목은 조정폭 축소, 저변동 품목은 빠르게 반영 |

**쓸 만한 개념**: 이동평균 기반 추세 판단, 모멘텀 약화/강화, 변동성 기반 신호 조절, 레짐 전환 탐지, 백테스트
**쓰면 안 되는 것**: RSI/MACD를 그대로 신봉하기, 딥러닝부터 시작하기, 100% 자동 가격 변경

### 7.4 전략 선택층 — 점수화 시스템

시장 상태를 점수로 변환하고, 점수 합계로 정책을 선택한다.

#### 신호 → 점수 변환 (초안)

| 신호 | 조건 | 점수 |
|------|------|------|
| 추세 하락 | 최근 3일 연속 하락 | +2 |
| 급락 | 당일 매입가가 8일 평균 대비 -8% 이하 | +2 |
| 매출 둔화 | 직전3개월평균 상승률 10% 이하로 떨어짐 | +2 |
| 판매량 감소 | 최근 7일 판매량이 이전 7일 대비 감소 | +1 |
| 박스권 이탈 | 최근 3일 평균이 이전 5일 평균에서 ±N% 이탈 | +1 |
| 변곡점 확인 | 이전 구간 평균과 신규 구간 사이에 명확한 갭 | +1 |
| 주요 경쟁 품목 | is_key_item = TRUE | +1 |

#### 점수 → 정책 매핑 (초안)

| 점수 합계 | 정책 | 수익률 조정 |
|-----------|------|------------|
| 0~1점 | **마진 방어** — 기존 수익률 유지 또는 소폭 상향 | +0~+1%p |
| 2~3점 | **소극적 인하** — 수익률 소폭 하향 | -1%p |
| 4~5점 | **점유율 확대** — 수익률 하향 | -2%p |
| 6점 이상 | **공격적 인하** — 수익률 대폭 하향 | -3%p |

반대로 매입가 상승 + 매출 호조 시에는 **마이너스 점수**로 수익률 상향도 가능.

#### 반대 방향 신호 (수익률 상향)

| 신호 | 조건 | 점수 |
|------|------|------|
| 추세 상승 | 최근 3일 연속 상승 | -2 |
| 매출 호조 | 직전3개월평균 상승률 20% 이상 | -1 |
| 판매량 증가 | 최근 7일 판매량이 이전 대비 증가 | -1 |

### 7.5 계층형 파라미터 구조

공통 로직은 유지하되, 품목별로 파라미터를 다르게 가져간다. 작년 1년치 매입/매출 데이터 백테스트로 최적 파라미터를 탐색한다.

```
┌─────────────────────────────────────────┐
│         전체 공통 파라미터 (기본값)        │
│  · 추세 판정 기준: 3일 연속              │
│  · 급락 기준: -8%                        │
│  · 매출 둔화 기준: 상승률 10% 이하       │
│  · 최소 마진: 15%                        │
├─────────────────────────────────────────┤
│     품목군별 파라미터 (오버라이드)         │
│                                          │
│  엽채류(대파,양배추,쪽파...):            │
│  · 변동성 높음 → 급락 기준 -12%로 완화   │
│  · 계절 영향 큼 → 추세 판정 4일로 연장   │
│                                          │
│  근채류(무,양파...):                     │
│  · 변동성 낮음 → 급락 기준 -5%로 강화    │
│  · 가격 안정적 → 조정폭 더 민감하게       │
│                                          │
│  버섯류:                                 │
│  · 가격 변동 작음 → 보합 구간 넓게        │
│  · 수요 안정적 → 매출 둔화 기준 5%로 강화│
├─────────────────────────────────────────┤
│     상품별 예외 (특수 케이스)             │
│                                          │
│  · 알배기: 계절 품목, 겨울철 기준 별도    │
│  · 특정 행사 품목: 점수 무시, 고정가 적용 │
│  · 신규 상품: 데이터 부족 시 품목군 기본값│
└─────────────────────────────────────────┘
```

### 7.6 백테스트 전략

작년(2025년) 1년치 상품별 매입가/매출 데이터로 엔진을 검증한다.

#### 백테스트 목적
- 점수화 시스템의 임계값(급락 -8%, 매출둔화 10% 등)이 실제로 유효한지 확인
- 품목군별 파라미터 차이가 실제 데이터에서 드러나는지 확인
- "이 엔진이 작년에 돌았다면 형민님의 실제 판매가와 얼마나 비슷했을까?"를 비교

#### 백테스트 흐름

```
[작년 일별 매입가 데이터] + [작년 일별 판매가 데이터]
         │                           │
         ▼                           │
  신호 해석층에 넣기                   │
  (추세, 변곡점, 변동성 계산)          │
         │                           │
         ▼                           │
  전략 선택층에서 점수 산출             │
         │                           │
         ▼                           │
  추천 판매가 산출                     │
         │                           │
         ▼                           ▼
  [엔진 추천가]  vs  [형민님 실제 판매가]
         │
         ▼
  일치율/괴리율 분석
  · 상품별: 어떤 품목에서 차이가 큰가?
  · 시기별: 어떤 상황에서 엔진이 틀렸는가?
  · 방향별: 엔진이 더 보수적이었나 공격적이었나?
```

#### 백테스트 성공 기준
- 엔진 추천가와 실제 판매가의 **방향 일치율 80% 이상** (올릴 때 올리고, 내릴 때 내렸는가)
- 가격 괴리가 **±5% 이내인 날이 70% 이상**
- 나머지 30%에서 "왜 달랐는지"를 분석하여 규칙 보완

### 7.7 개발 단계에 백테스트 반영

기존 5.4의 스프린트 계획에 백테스트 단계를 추가한다.

#### 0단계 (신규): 백테스트로 엔진 설계 검증

| 스프린트 | 제목 | 핵심 합격 조건 |
|----------|------|----------------|
| S00-A | 작년 데이터 수집 + DB 적재 | 2025년 1년치 일별 매입가/판매가를 Supabase에 적재. 상품 700개+ |
| S00-B | 신호 해석층 백테스트 | 작년 데이터로 추세/변곡점/변동성을 산출. 형민님이 "이 판정이 맞다"고 확인할 수 있는 리포트 |
| S00-C | 점수화 + 추천가 백테스트 | 작년 데이터로 추천가 산출 → 실제 판매가와 비교. 방향 일치율/괴리율 리포트 |
| S00-D | 품목군별 파라미터 튜닝 | 백테스트 결과에서 괴리가 큰 품목군의 파라미터 조정. 재테스트 후 일치율 개선 확인 |

이 0단계를 먼저 돌려서 엔진의 기본 설계가 맞는지 확인한 뒤, 기존 S01~S16 개발에 들어간다.

### 7.8 2025년 데이터 분석 결과 (백테스트 기초)

2025년 천년경영 매입상세/매출상세 엑셀 데이터를 분석한 결과:

#### 데이터 규모

| 구분 | 값 |
|------|------|
| 매입 건수 | 65,535건 |
| 매출 건수 | 60,486건 |
| 매입 고유 상품코드 | 497개 |
| 매출 고유 상품코드 | 672개 |
| 양쪽 모두 있는 코드 | 331개 |
| 매출만 있는 코드 (소분/재고판매) | **341개** |
| 매입만 있는 코드 | 166개 |
| 기간 | 2025-01-02 ~ 2025-12-31 (매입 298일, 매출 294일) |
| 하루 다중 단가 판매 건수 | 4,246건 (하루에 여러 번 가격 변경) |

#### 상품그룹 동조화 — 소분상품 구조 확인

매출만 있고 매입이 없는 341개 상품 중 상당수가 **원물을 소분하여 판매하는 상품**이다.

| 원물 (매입 있음) | 소분상품 (매출만 있음) |
|-----------------|---------------------|
| 가지/특(003362, 박스5kg) | 가지 3개(013899), 5개(013900), 10개(013901) |
| 청오이(매입코드) | 청오이 3개, 5개, 10개 |
| 백오이(매입코드) | 백오이 3개, 5개, 10개 |
| 팽이버섯(003006 등) | 팽이버섯 3봉, 5봉, 10봉 |
| 애호박(매입코드) | 애호박 3개, 5개, 10개 |
| 주키니호박(매입코드) | 주키니호박 3개, 5개, 10개 |

→ 소분상품은 원물의 매입가 변동을 반드시 추적해야 함. 엔진에서 **원물→소분 매핑 테이블**이 필요.

#### 주요 경쟁 품목별 상품그룹 현황

| 품목 | 총 상품수 | 매입 있음 | 매입 없음(소분) | 매입 빈도 최다 |
|------|----------|----------|----------------|--------------|
| 대파 | 8개 | 3 | 5 | 굵은대파大(414건) |
| 양배추 | 6개 | 3 | 3 | 양배추/낱개(348건) |
| 알배기 | 5개 | 2 | 3 | 알배기/국내산(317건) |
| 미나리 | 3개 | 3 | 0 | 돌미나리A급(306건) |
| 쪽파 | 5개 | 3 | 2 | 깐쪽파大(294건) |
| 버섯 | 33개 | 13 | 20 | 팩느타리200g(296건) |
| 양파 | 16개 | 6 | 10 | 양파12kg/2번(125건) |
| 양상추 | 5개 | 2 | 3 | 양상추/수입(179건) |

→ 버섯은 33개 상품 중 20개가 소분. 양파는 16개 중 10개가 소분. 품목별로 구조가 매우 다르다.

#### 가지 그룹 3월 실데이터 — 동조화 필요성 실증

```
가지/상(005045) 매입가:  20000→18000→25000→26000→25000→24000→22000→...→14000→15000
가지/특(003362) 매입가:  25000→23000→30000→32000→30000→28000→27000→...→20000→20000
가지1kg(004046) 매입가:  7000→7000→7400→7400→7400→...→6800→6600→5800→6600→6400

가지/상(005045) 판매가:  26780→26780→31270→31270→31270→29870→24780→22470→...→20470
가지3개(013899) 판매가:  3280→3780→3780→3180→2980  (매입 없음, 원물 추세 따라감)
가지5개(013900) 판매가:  6370→6370→5980→5980→4870→4780→4870→4970→4780 (매입 없음)
```

→ 가지/상·가지/특은 3월 중 유사한 등락 패턴 (3/7-8 급등 → 3/15-18 급락 → 3/20 이후 보합)
→ 소분상품(3개, 5개)도 같은 추세를 반영하여 판매가가 내려감
→ **이것이 상품그룹 동조화의 실제 데이터 근거**

---

## 6. 확인/결정 필요 사항

- [x] 주요 경쟁 품목 목록 확정 → **무, 대파, 양배추, 쪽파, 양파, 양상추, 미나리, 버섯, 알배기** (웹에서 수정 가능하도록 구현)
- [x] 플랫폼별 상품코드 매핑 데이터 확보 → **★ 플랫폼 시트에 숨김 시트로 존재**: 배민전체상품, 온일장전체상품, 신선행전체상품
- [x] 수익률일괄변경용 목표수익률 → 고정값이 아닌 **의사결정 엔진이 동적으로 추천하는 값**으로 전환 (섹션 7 참조). 현재 구글시트의 목표수익률은 초기 기본값으로 DB에 이전하되, 이후 엔진이 시장 상태에 따라 추천 수익률을 산출
- [ ] Supabase 프로젝트 생성 및 초기 설정
- [ ] 신선행 API 문서/엔드포인트 확인
- [ ] Vercel 또는 Railway 중 배포 플랫폼 결정
- [ ] 작년(2025년) 1년치 상품별 매입가/매출 데이터 확보 → 백테스트용



================================================================================
# [파일 구분선] 아래부터 CLAUDE.md
================================================================================


# 야채 판매가 자동책정 시스템 — Claude Code 프로젝트 가이드

## 프로젝트 개요

식자재 유통업체의 야채 판매가를 4개 플랫폼(식봄, 온일장, 신선행, 배민)에 자동으로 책정하는 시스템.
현재 구글시트 기반 수동 작업을 → Supabase + Next.js 웹 시스템으로 전환하는 프로젝트.

핵심 아키텍처: **3층 의사결정 엔진** (Signal → Strategy → Price)
- 신호 해석층: 시장 상태 판정 (추세, 변곡점, 변동성)
- 전략 선택층: 점수화 → 정책 선택 (마진방어/현상유지/점유율확대/공격적인하)
- 가격 산출층: 구체적 추천가 산출

**3단계 하네스 프로토콜**: 기획자 → 생성자 → 평가자 (GAN 구조)

## 기술 스택

| 구성요소 | 기술 |
|----------|------|
| DB | Supabase (PostgreSQL) — 프로젝트 ID: `sxndahqadpgivvejxjtg`, 리전: ap-northeast-2 |
| 프론트엔드 | Next.js (App Router) + TypeScript + Tailwind CSS |
| 배포 | Vercel |
| 코드관리 | GitHub |

## 현재 진행 상태 (2026-04-01 기준)

### S00-A: 2025년 데이터 Supabase 적재 — 진행 중

| 테이블 | 목표 | 현재 | 진행률 | 비고 |
|--------|------|------|--------|------|
| products | 838 | 838 | **100%** | 완료 |
| daily_purchase_prices | 65,535 | 25,349 | **39%** | SQL 배치 실행 필요 |
| daily_selling_prices | 60,486 | 9,587 | **16%** | SQL 배치 실행 필요 |

### 즉시 해야 할 작업: 데이터 로딩 완료

`claude-code-migration/` 폴더에 준비된 SQL 배치 파일을 실행하면 됨:

```bash
# 1. Supabase 연결 문자열 확인 (Supabase Dashboard → Settings → Database → Connection string)
# 형식: postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres

# 2. 매입 데이터 로딩 (188 파일, ON CONFLICT DO NOTHING으로 안전)
for f in claude-code-migration/p_sql/*.sql; do
  psql "$DATABASE_URL" -f "$f" 2>/dev/null
  echo "Done: $f"
done

# 3. 매출 데이터 로딩 (173 파일, ON CONFLICT DO NOTHING으로 안전)
for f in claude-code-migration/s_sql/*.sql; do
  psql "$DATABASE_URL" -f "$f" 2>/dev/null
  echo "Done: $f"
done

# 4. 검증
psql "$DATABASE_URL" -c "SELECT 'products' as t, count(*) FROM products UNION ALL SELECT 'purchases', count(*) FROM daily_purchase_prices UNION ALL SELECT 'sales', count(*) FROM daily_selling_prices;"
# 기대값: products=838, purchases=65535, sales=60486
```

> **참고**: SQL 파일들은 모두 ON CONFLICT DO NOTHING 포함이므로 여러 번 실행해도 안전합니다.
> psql이 없으면 `npm install -g supabase` 후 Supabase MCP를 사용하거나,
> load_data.sh 스크립트를 참고하세요.

## 데이터베이스 스키마 (현재 Supabase에 존재)

```sql
-- 상품 마스터 (838행)
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) UNIQUE NOT NULL,  -- 천년경영 상품코드
  product_name VARCHAR(200) NOT NULL,
  spec VARCHAR(100),                        -- 규격
  unit VARCHAR(20),                         -- 단위
  category_name VARCHAR(100),               -- 소분류명
  product_group INTEGER,                    -- 상품그룹 번호
  is_key_item BOOLEAN DEFAULT FALSE,        -- 주요 경쟁품목 여부
  is_event_item BOOLEAN DEFAULT FALSE,      -- 행사품목 여부
  target_margin_rate DECIMAL(5,2),          -- 목표수익률
  unit_type VARCHAR(20),
  platform_codes JSONB,                     -- 플랫폼별 상품코드 매핑
  parent_product_code VARCHAR(6),           -- 소분상품의 원물 코드
  has_purchase_history BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 일별 매입가 (목표: 65,535행)
CREATE TABLE daily_purchase_prices (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) NOT NULL REFERENCES products(product_code),
  price_date DATE NOT NULL,
  purchase_price INTEGER NOT NULL,
  quantity DECIMAL(10,2),
  unit VARCHAR(20),
  supply_amount INTEGER,
  UNIQUE(product_code, price_date, purchase_price)
);

-- 일별 매출가 (목표: 60,486행)
CREATE TABLE daily_selling_prices (
  id SERIAL PRIMARY KEY,
  product_code VARCHAR(6) NOT NULL REFERENCES products(product_code),
  price_date DATE NOT NULL,
  selling_price INTEGER NOT NULL,
  quantity DECIMAL(10,2),
  unit VARCHAR(20),
  supply_amount INTEGER,
  CONSTRAINT uq_selling UNIQUE(product_code, price_date, selling_price)
);
```

## 스프린트 로드맵

### 0단계: 백테스트 (현재)
- **S00-A**: 2025 데이터 Supabase 적재 ← **현재 여기**
- **S00-B**: 신호 해석층 백테스트 (추세/변곡점/변동성 계산)
- **S00-C**: 점수화 + 추천가 백테스트 (실제 판매가와 비교)
- **S00-D**: 품목군별 파라미터 튜닝

### 1단계: 기반 구축
- S01: Supabase DB 테이블 + Next.js 프로젝트
- S02: 엑셀 업로드 API (천년경영 → DB)
- S03: 기존 데이터 마이그레이션

### 2단계: 분석 엔진
- S05~S08: 매입가 변동, 7일 동향, 판매량 추이, 추천 판매가 산출

### 3단계: 웹 대시보드
- S09~S12: 전체상품 뷰, 인라인 수정, 추천가 UI, 상품그룹 동조화

### 4단계: 플랫폼 업로드
- S13~S16: 식봄/온일장/배민/신선행 업로드 파일 생성

## 핵심 비즈니스 규칙

### 판매가 계산 공식
- **수익률** = 1 - (매입가 ÷ 판매가)
- **수익률일괄변경가** = ROUNDUP(매입가 ÷ (1 - 목표수익률), -1)  ← 10원 단위 올림
- **신선행판매가** = MAX(식봄판매가 × 0.94, 매입가 ÷ 0.9)
- **배민판매가** = 식봄판매가 (동일)
- **행사품목**: 판매가 × 1.25 = 정상가

### 상품그룹 동조화
- 같은 그룹 내 매입빈도 최다 상품 = 대표상품
- 소분상품(매출만 있음)은 원물의 매입가 추세를 따라감
- parent_product_code로 원물↔소분 매핑

### 주요 경쟁 품목
무, 대파, 양배추, 쪽파, 양파, 양상추, 미나리, 버섯, 알배기

## 기본 원칙
- 판매가 계산 수식은 기획서(판매가_자동화_기획서.md) 섹션 2.1과 정확히 일치해야 한다
- 판단 기반 규칙(섹션 2.2)은 "추천"으로만 표시, 자동 확정 불가
- 형민님이 확정 버튼을 누르기 전까지 판매가 변경 없음
- 금액은 모두 정수(원 단위), ROUNDUP(-1)은 10원 단위 올림

## 하지 말 것
- .env 파일에 직접 시크릿 하드코딩 금지
- 판매가를 자동으로 플랫폼에 전송하지 않기 (반드시 사람 확인 후)
- 스프린트 계약서에 없는 기능 추가하지 않기

## 파일 구조

```
판매가변경영상/
├── 판매가_자동화_기획서.md              ← 전체 설계 문서 (필독)
├── CLAUDE.md                           ← 이 파일
├── claude-code-migration/
│   ├── CLAUDE.md                       ← 이 파일 사본
│   ├── p_sql/                          ← 매입 INSERT SQL (188 파일, 350행/파일)
│   ├── s_sql/                          ← 매출 INSERT SQL (173 파일, 350행/파일)
│   ├── load_data.sh                    ← 데이터 로딩 bash 스크립트
│   └── .mcp.json                       ← Supabase MCP 설정 (참고용)
├── sprints/                            ← 스프린트 계약서/평가서 (향후 생성)
└── src/                                ← Next.js 소스 (향후 생성)
```

## 데이터 원본

2025년 천년경영 엑셀:
- 매입상세: 65,535건, 497개 고유 상품코드
- 매출상세: 60,486건, 672개 고유 상품코드
- 양쪽 모두 있는 코드: 331개
- 매출만 있는 코드 (소분/재고판매): 341개
- 기간: 2025-01-02 ~ 2025-12-31



================================================================================
# [파일 구분선] 아래부터 스프린트 계약서들
================================================================================


# 스프린트 S00-B: 신호 해석층 백테스트

## 목표
2025년 매입가 데이터로 신호 해석층(Signal Layer)의 추세/변곡점/변동성을 산출하고, 형민님이 "이 판정이 맞다"고 확인할 수 있는 리포트를 생성한다.

## 구현 범위
- [ ] 신호 해석층 SQL 함수 구현 (Supabase PostgreSQL)
  - 8일 절사평균 동향 (급등/상승/보합/하락/급락 5단계)
  - 최근 3일 추세 방향 및 지속 일수
  - 변곡점 매입가 (박스권 이탈 시점)
  - 7일 변동성 수준 (표준편차/평균 기반 CV)
  - 7일 최고가/중앙값/가중평균
- [ ] 백테스트 실행: 주요 경쟁 품목 대상으로 2025년 전 기간 신호 산출
- [ ] 검증 리포트 생성: 가지 그룹 3월 데이터로 신호 판정 결과 확인

## 제외 범위
- 전략 선택층 (점수화) — S00-C에서 구현
- 추천 판매가 산출 — S00-C에서 구현
- 매출 추이 분석 — S00-C에서 구현
- 웹 UI — S09 이후

## 합격 조건 (Acceptance Criteria)
1. **절사평균 동향 일치**: 8일 매입가 기반 절사평균 5단계 판정이 기획서 섹션 2.1의 로직과 정확히 일치
   - 변동률 기준: +20% 초과=급등, +3%~+20%=상승, ±3%=보합, -3%~-20%=하락, -20% 초과=급락
   - 매입 4일 미만 시 계산 제외
2. **변곡점 탐지**: 가지 그룹 3월 데이터에서 3/7-8 급등, 3/15-18 급락 변곡점이 탐지됨
3. **리포트 가독성**: 상품별 일별 신호 결과를 형민님이 확인 가능한 형태로 출력

## 참조 자료
- 기획서 섹션 2.1: 절사평균 동향 분석 로직
- 기획서 섹션 7.2~7.3: 신호 해석층 설계
- 기획서 섹션 7.8: 가지 그룹 3월 실데이터

## 검증 방법
- SQL 쿼리로 가지/상(005045), 가지/특(003362) 3월 데이터에서 신호 산출 → 기획서 7.8 실데이터와 대조
- 주요 경쟁 품목 9개에 대해 전체 기간 신호 산출 → 이상치 없는지 확인


---


# 스프린트 S00-C: 점수화 + 추천가 백테스트

## 목표
S00-B에서 만든 신호 해석층 위에 전략 선택층(점수화)과 가격 산출층을 구현하고, 2025년 실제 판매가와 비교하여 엔진의 유효성을 검증한다.

## 구현 범위
- [ ] 매출 추이 분석 함수 (직전3개월 평균 매출 vs 당월)
- [ ] 전략 선택층: 신호 → 점수 변환 → 정책 선택 (마진방어/현상유지/점유율확대/공격적인하)
- [ ] 가격 산출층: 기준매입가 + 추천수익률 → 추천판매가 산출
- [ ] 통합 백테스트: 엔진 추천가 vs 실제 판매가 비교 리포트

## 제외 범위
- 품목군별 파라미터 튜닝 — S00-D에서 수행
- 웹 UI — S09 이후

## 합격 조건
1. 점수화 시스템이 기획서 섹션 7.4의 신호→점수 변환표와 일치
2. 추천가 산출이 기획서 섹션 2.1의 수식(ROUNDUP, 10원단위 올림)과 정확히 일치
3. 엔진 추천가와 실제 판매가의 **방향 일치율 80% 이상**, **가격 괴리 ±5% 이내 70% 이상**

## 참조 자료
- 기획서 섹션 2.1: 판매가 계산 공식
- 기획서 섹션 2.2: 규칙 1~8
- 기획서 섹션 7.4: 점수화 시스템
- 기획서 섹션 7.6: 백테스트 성공 기준

## 대상 상품
- 567개 (DB에 존재하는 활성 상품)


---


# 스프린트 S00-D: 품목군별 파라미터 튜닝

## 목표
S00-C 백테스트에서 발견된 구조적 괴리(수익률 미설정, 시차 미반영, 품목군 미분화)를 해결하고, 엔진 성공 기준(방향 일치율 80%, 괴리 ±5% 70%)을 달성한다.

## 구현 범위
- [ ] 상품별 target_margin_rate 역산 설정 (실제 운영 수익률 기반)
- [ ] 시차(lag) 적용 방향 일치율 재측정 (D+1, D+2, D+3)
- [ ] 품목군별 파라미터 분화 (엽채류/근채류/버섯류/가공품 등)
- [ ] 재백테스트: 튜닝 후 성공 기준 재평가

## 합격 조건
1. target_margin_rate가 567개 활성 상품에 설정됨
2. 튜닝 후 방향 일치율 80% 이상 (시차 포함)
3. 튜닝 후 가격 괴리 ±5% 이내 70% 이상


---


# 스프린트 S01: Next.js 프로젝트 생성 + Supabase 연동

## 목표
Next.js 프로젝트를 생성하고 Supabase와 연동하여, 빈 대시보드 페이지가 localhost:3000에서 열리는 것을 확인한다.

## 구현 범위
- [ ] Next.js 프로젝트 생성 (App Router + TypeScript + Tailwind CSS)
- [ ] Supabase 클라이언트 설정 (@supabase/supabase-js)
- [ ] 환경변수 설정 (.env.local)
- [ ] 메인 대시보드 페이지 (빈 레이아웃 + DB 연결 확인)
- [ ] GitHub 리포지토리 생성 + push

## 제외 범위
- 엑셀 업로드 기능 — S02
- 분석 엔진 UI — S09 이후
- Vercel 배포 — 별도 진행

## 합격 조건
1. localhost:3000에서 대시보드 페이지가 열림
2. Supabase에서 products 테이블 데이터를 읽어서 상품 수(838개)가 화면에 표시됨
3. GitHub에 push 완료


---


# 스프린트 S02: 엑셀 업로드 API (천년경영 → DB)

## 목표
천년경영 프로그램에서 다운로드한 매입상세/매출상세 엑셀 파일을 웹에서 드래그앤드롭으로 업로드하면 Supabase DB에 자동 저장되도록 한다.

## 구현 범위
- [ ] 엑셀 파싱 라이브러리 설치 (xlsx)
- [ ] 업로드 페이지 UI (/upload)
- [ ] 매입상세 엑셀 파싱 → daily_purchase_prices INSERT API
- [ ] 매출상세 엑셀 파싱 → daily_selling_prices INSERT API
- [ ] 업로드 결과 표시 (성공/실패 건수)

## 제외 범위
- 상품 마스터 자동 등록 (이미 838개 등록됨)
- 플랫폼별 업로드 파일 생성 — S13 이후

## 합격 조건
1. /upload 페이지에서 매입상세 엑셀을 드래그앤드롭하면 daily_purchase_prices에 데이터가 저장됨
2. 매출상세 엑셀도 동일하게 동작
3. 중복 데이터는 ON CONFLICT로 안전하게 처리
4. 업로드 후 성공/실패 건수가 화면에 표시됨


---


# 스프린트 S09: 전체상품 테이블 뷰 (읽기 전용)

## 목표
야채 전체상품을 브라우저 테이블에 표시하고, 상품그룹별 정렬/필터, 매입가/판매가/수익률을 한눈에 볼 수 있는 대시보드를 구현한다.

## 구현 범위
- [ ] /products 페이지: 전체상품 테이블 뷰
- [ ] 컬럼: 상품코드, 상품명, 대분류, 상품그룹, 최근매입가, 최근판매가, 수익률, 목표수익률, 동향(5단계), 변동성
- [ ] 상품그룹별 그룹핑/정렬
- [ ] 대분류별 필터 (엽채류/근채류/과채류/버섯류/가공품 등)
- [ ] 수익률 19.5% 미만 하이라이트 (노란색)
- [ ] 급등/급락 하이라이트 (빨간색)
- [ ] 검색 (상품명/상품코드)

## 합격 조건
1. /products 페이지에서 567개 이상 상품이 테이블에 표시됨
2. 수익률 19.5% 미만 상품이 노란색으로 하이라이트
3. 대분류 필터와 검색이 동작


---


# 스프린트 S10: 판매가 인라인 수정 + 확정

## 목표
전체상품 테이블에서 판매가를 직접 수정하고 확정 버튼으로 DB에 저장할 수 있도록 한다.

## 구현 범위
- [ ] 판매가 셀 클릭 → 인라인 편집 모드
- [ ] 수익률 자동 재계산 (편집 시 실시간)
- [ ] 확정 버튼 → DB 저장 API
- [ ] 추천가 표시 + 이상치 하이라이트 (S11 병합)
- [ ] 상품그룹 동조화 보조 UI (S12 병합)

## 합격 조건
1. 판매가 셀 클릭 → 수정 → 확정 → DB 저장 확인
2. 수정 시 수익률이 실시간 재계산됨
3. 추천가 열이 표시되고 급등/급락 하이라이트


---


# 스프린트 S13: 식봄 업로드 엑셀 생성

## 목표
확정된 판매가 데이터를 식봄 플랫폼 업로드 양식에 맞는 엑셀 파일로 생성/다운로드한다.

## 구현 범위
- [ ] /api/platform/sikbom GET → 식봄 업로드용 엑셀 생성
- [ ] 컬럼: 상품코드, 상품명, 규격, 판매가, 정상가(행사품목), 행사여부
- [ ] 행사품목: 판매가 × 1.25 = 정상가
- [ ] 금액 10원 단위 올림 (ROUNDUP -1)

## 합격 조건
1. API 호출 시 .xlsx 파일이 정상 다운로드됨
2. 행사품목의 정상가가 판매가 × 1.25로 계산됨
3. 전체 상품 데이터가 누락 없이 포함됨


---


# 스프린트 S14: 온일장 + 배민 업로드 엑셀

## 목표
온일장과 배민 플랫폼에 업로드할 엑셀 파일을 각각 생성한다.

## 구현 범위
- [ ] /api/platform/baemin GET → 배민 업로드용 엑셀 (판매가 = 식봄가 동일)
- [ ] /api/platform/oniljang GET → 온일장 업로드용 엑셀 (식봄가 기반)
- [ ] 각 플랫폼별 컬럼 구성

## 합격 조건
1. 배민 엑셀: 판매가 = 식봄 판매가와 동일
2. 온일장 엑셀: 식봄 기반 가격 포함
3. 두 파일 모두 정상 다운로드


---


# 스프린트 S15: 신선행 API 연동

## 목표
신선행(자사 시스템)에 판매가를 API로 직접 전송할 수 있도록 한다.

## 구현 범위
- [ ] /api/platform/sinsunhang POST → 신선행 판매가 데이터 조회/전송 준비
- [ ] 신선행 판매가 = MAX(식봄가 × 0.94, 매입가 ÷ 0.9)
- [ ] 전송 대상 상품 목록 + 가격 미리보기

## 합격 조건
1. 신선행 판매가가 올바르게 계산됨
2. 전송 대상 상품 목록이 JSON으로 반환됨
3. 식봄 수익률 11.1% 미만 시 하한선(매입가/0.9)이 적용됨


---


# 스프린트 S16: 통합 업로드 + 다운로드 UI

## 목표
한 화면에서 4개 플랫폼 업로드 파일을 생성/다운로드할 수 있는 통합 UI를 구현한다.

## 구현 범위
- [ ] /platform 페이지: 4개 플랫폼 카드 UI
- [ ] 각 플랫폼별 다운로드 버튼 (식봄/배민/온일장 → 엑셀, 신선행 → API)
- [ ] 다운로드 전 상품 수/변경 상품 수 미리보기
- [ ] 내비게이션 바에 "플랫폼 업로드" 링크 추가

## 합격 조건
1. /platform 페이지에서 4개 플랫폼 카드가 표시됨
2. 식봄/배민/온일장 다운로드 버튼 클릭 시 엑셀 파일이 다운로드됨
3. 신선행 카드에서 전송 대상 미리보기가 표시됨



================================================================================
# [파일 구분선] 아래부터 SQL 함수: S00-C 전략 선택층 (strategy_score_policy)
================================================================================


-- ============================================================
-- S00-C: 전략 선택층 (점수화 시스템) SQL 함수들
-- 생성일: 2026-04-01
-- Supabase 프로젝트: sxndahqadpgivvejxjtg
-- ============================================================

-- ============================================================
-- Part 1: signal_sales_trend — 매출 추이 분석
-- ============================================================
-- 직전 3개월 중 매출 있는 월만 계산
-- 각 월의 일평균 판매량 → 평균 → 당월 진행일수 기준 기대치
-- 당월 실제 판매량과 비교하여 등락률(%) 반환
-- ============================================================

CREATE OR REPLACE FUNCTION signal_sales_trend(
  p_product_code VARCHAR(6),
  p_date DATE
)
RETURNS TABLE(
  expected_qty NUMERIC,
  actual_qty NUMERIC,
  change_rate NUMERIC,
  label VARCHAR
)
LANGUAGE plpgsql
AS $function$
DECLARE
  v_current_month_start DATE;
  v_day_of_month INT;
  v_days_in_month INT;
  v_actual_qty NUMERIC;
  v_prev_months_data RECORD;
  v_total_daily_avg NUMERIC := 0;
  v_month_count INT := 0;
  v_expected_qty NUMERIC;
  v_change_rate NUMERIC;
  v_m DATE;
BEGIN
  v_current_month_start := DATE_TRUNC('month', p_date)::DATE;
  v_day_of_month := EXTRACT(DAY FROM p_date)::INT;
  v_days_in_month := (EXTRACT(DAY FROM (DATE_TRUNC('month', p_date) + INTERVAL '1 month - 1 day')))::INT;

  -- Actual sales quantity in current month up to p_date
  SELECT COALESCE(SUM(s.quantity), 0)
  INTO v_actual_qty
  FROM daily_selling_prices s
  WHERE s.product_code = p_product_code
    AND s.price_date >= v_current_month_start
    AND s.price_date <= p_date;

  -- Check previous 3 months
  FOR i IN 1..3 LOOP
    v_m := (v_current_month_start - (i || ' months')::INTERVAL)::DATE;

    SELECT
      SUM(s.quantity) as total_qty,
      (EXTRACT(DAY FROM (v_m + INTERVAL '1 month - 1 day')))::INT as days_in_month
    INTO v_prev_months_data
    FROM daily_selling_prices s
    WHERE s.product_code = p_product_code
      AND s.price_date >= v_m
      AND s.price_date < (v_m + INTERVAL '1 month')::DATE;

    IF v_prev_months_data.total_qty IS NOT NULL AND v_prev_months_data.total_qty > 0 THEN
      v_total_daily_avg := v_total_daily_avg + (v_prev_months_data.total_qty / v_prev_months_data.days_in_month);
      v_month_count := v_month_count + 1;
    END IF;
  END LOOP;

  IF v_month_count = 0 THEN
    IF v_actual_qty > 0 THEN
      RETURN QUERY SELECT 0::NUMERIC, v_actual_qty, NULL::NUMERIC, '신규매출'::VARCHAR;
    ELSE
      RETURN QUERY SELECT 0::NUMERIC, 0::NUMERIC, NULL::NUMERIC, NULL::VARCHAR;
    END IF;
    RETURN;
  END IF;

  -- Scale expected by day progress in current month
  v_expected_qty := (v_total_daily_avg / v_month_count) * v_day_of_month;

  IF v_expected_qty = 0 THEN
    v_change_rate := NULL;
  ELSE
    v_change_rate := ROUND(((v_actual_qty - v_expected_qty) / v_expected_qty) * 100, 1);
  END IF;

  RETURN QUERY SELECT
    ROUND(v_expected_qty, 1),
    v_actual_qty,
    v_change_rate,
    CASE
      WHEN v_change_rate IS NULL THEN NULL
      WHEN v_change_rate > 0 THEN ('▲' || ABS(v_change_rate) || '%')::VARCHAR
      WHEN v_change_rate < 0 THEN ('▼' || ABS(v_change_rate) || '%')::VARCHAR
      ELSE '동일'::VARCHAR
    END;
END;
$function$;


-- ============================================================
-- Part 2: strategy_score_policy — 점수화 + 정책 선택
-- ============================================================
-- 신호 해석층(S00-B) 함수들의 결과를 점수로 변환
-- 점수 합계로 6단계 정책 선택
--
-- 하락(인하) 방향 → 양수 점수:
--   추세하락(+2), 급락(+2), 매출둔화(+2), 판매량감소(+1),
--   박스권이탈-하방(+1), 변곡점확인(+1), 주요경쟁품목(+1)
-- 상승(인상) 방향 → 음수 점수:
--   추세상승(-2), 급등(-2), 매출호조(-1), 판매량증가(-1),
--   박스권이탈-상방(-1)
--
-- 점수 → 정책:
--   <= -3: 마진상향 (+1.5%p)
--   -2~-1: 소극적상향 (+0.75%p)
--   0~1:   현상유지 (0)
--   2~3:   소극적인하 (-1%p)
--   4~5:   점유율확대 (-2%p)
--   >= 6:  공격적인하 (-3%p)
-- ============================================================

CREATE OR REPLACE FUNCTION strategy_score_policy(
  p_product_code VARCHAR(6),
  p_date DATE
)
RETURNS TABLE(
  total_score INT,
  policy VARCHAR,
  margin_adjustment NUMERIC,
  score_details JSONB
)
LANGUAGE plpgsql
AS $function$
DECLARE
  v_score INT := 0;
  v_details JSONB := '[]'::JSONB;

  -- Signal results
  v_trend RECORD;
  v_consec RECORD;
  v_inflec RECORD;
  v_volatility RECORD;
  v_sales RECORD;
  v_is_key_item BOOLEAN;

  -- Latest price vs box
  v_latest_price INT;
  v_latest_gap_rate NUMERIC;
  v_effective_inflection_dir VARCHAR;

  v_policy VARCHAR;
  v_margin_adj NUMERIC;
BEGIN
  -- Gather all signals
  SELECT * INTO v_trend FROM signal_trimmed_mean_trend(p_product_code, p_date);
  SELECT * INTO v_consec FROM signal_consecutive_trend(p_product_code, p_date);
  SELECT * INTO v_inflec FROM signal_inflection_point(p_product_code, p_date);
  SELECT * INTO v_volatility FROM signal_volatility(p_product_code, p_date);
  SELECT * INTO v_sales FROM signal_sales_trend(p_product_code, p_date);

  SELECT p.is_key_item INTO v_is_key_item
  FROM products p WHERE p.product_code = p_product_code;

  -- Get the latest purchase price on or before p_date
  SELECT purchase_price INTO v_latest_price
  FROM daily_purchase_prices
  WHERE product_code = p_product_code AND price_date <= p_date
  ORDER BY price_date DESC LIMIT 1;

  -- Determine effective inflection direction based on LATEST price vs box
  -- This corrects cases where first breakout was down but latest price surged up
  IF v_inflec.box_avg IS NOT NULL AND v_inflec.box_avg > 0 AND v_latest_price IS NOT NULL THEN
    v_latest_gap_rate := (v_latest_price - v_inflec.box_avg) / v_inflec.box_avg;
    IF v_latest_gap_rate > 0.05 THEN
      v_effective_inflection_dir := '상방이탈';
    ELSIF v_latest_gap_rate < -0.05 THEN
      v_effective_inflection_dir := '하방이탈';
    ELSE
      v_effective_inflection_dir := NULL;
    END IF;
  ELSE
    v_effective_inflection_dir := v_inflec.inflection_direction;
  END IF;

  -- ===== DOWNWARD (price cut) signals = positive scores =====

  -- 1. 추세 하락: consecutive_trend = '하락' AND consecutive_days >= 3 -> +2
  IF v_consec.direction = '하락' AND v_consec.consecutive_days >= 3 THEN
    v_score := v_score + 2;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '추세하락', 'score', 2,
      'detail', v_consec.direction || ' ' || v_consec.consecutive_days || '일 연속'
    ));
  END IF;

  -- 2. 급락: trimmed_mean_trend = '급락' -> +2
  IF v_trend.trend = '급락' THEN
    v_score := v_score + 2;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '급락', 'score', 2,
      'detail', '절사평균 변동률 ' || ROUND(v_trend.change_rate * 100, 1) || '%'
    ));
  END IF;

  -- 3. 매출 둔화: sales change_rate > 0 AND <= 10 -> +2
  IF v_sales.change_rate IS NOT NULL AND v_sales.change_rate > 0 AND v_sales.change_rate <= 10 THEN
    v_score := v_score + 2;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '매출둔화', 'score', 2,
      'detail', '매출 증가율 ' || v_sales.change_rate || '% (둔화)'
    ));
  END IF;

  -- 4. 판매량 감소: sales change_rate < 0 -> +1
  IF v_sales.change_rate IS NOT NULL AND v_sales.change_rate < 0 THEN
    v_score := v_score + 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '판매량감소', 'score', 1,
      'detail', '매출 변화율 ' || v_sales.change_rate || '%'
    ));
  END IF;

  -- 5. 박스권 이탈(하방) -> +1
  IF v_effective_inflection_dir = '하방이탈' THEN
    v_score := v_score + 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '박스권이탈(하방)', 'score', 1,
      'detail', '최근가 ' || v_latest_price || '원 vs 박스평균 ' || v_inflec.box_avg || '원 (갭률 ' || ROUND(v_latest_gap_rate * 100, 1) || '%)'
    ));
  END IF;

  -- 6. 변곡점 확인: inflection_price IS NOT NULL -> +1
  IF v_inflec.inflection_price IS NOT NULL THEN
    v_score := v_score + 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '변곡점확인', 'score', 1,
      'detail', '변곡일 ' || v_inflec.inflection_date || ', 방향 ' || COALESCE(v_inflec.inflection_direction, 'N/A')
    ));
  END IF;

  -- 7. 주요 경쟁 품목 -> +1
  IF v_is_key_item = TRUE THEN
    v_score := v_score + 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '주요경쟁품목', 'score', 1,
      'detail', '경쟁 우선 품목'
    ));
  END IF;

  -- ===== UPWARD (price raise) signals = negative scores =====

  -- 8. 추세 상승: consecutive >= 3일 -> -2
  IF v_consec.direction = '상승' AND v_consec.consecutive_days >= 3 THEN
    v_score := v_score - 2;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '추세상승', 'score', -2,
      'detail', v_consec.direction || ' ' || v_consec.consecutive_days || '일 연속'
    ));
  END IF;

  -- 9. 급등: trimmed_mean_trend = '급등' -> -2
  IF v_trend.trend = '급등' THEN
    v_score := v_score - 2;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '급등', 'score', -2,
      'detail', '절사평균 변동률 ' || ROUND(v_trend.change_rate * 100, 1) || '%'
    ));
  END IF;

  -- 10. 매출 호조: sales change_rate >= 20 -> -1
  IF v_sales.change_rate IS NOT NULL AND v_sales.change_rate >= 20 THEN
    v_score := v_score - 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '매출호조', 'score', -1,
      'detail', '매출 증가율 ' || v_sales.change_rate || '%'
    ));
  END IF;

  -- 11. 판매량 증가: sales change_rate > 0 -> -1
  IF v_sales.change_rate IS NOT NULL AND v_sales.change_rate > 0 THEN
    v_score := v_score - 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '판매량증가', 'score', -1,
      'detail', '매출 변화율 +' || v_sales.change_rate || '%'
    ));
  END IF;

  -- 12. 박스권 이탈(상방) -> -1
  IF v_effective_inflection_dir = '상방이탈' THEN
    v_score := v_score - 1;
    v_details := v_details || jsonb_build_array(jsonb_build_object(
      'signal', '박스권이탈(상방)', 'score', -1,
      'detail', '최근가 ' || v_latest_price || '원 vs 박스평균 ' || v_inflec.box_avg || '원 (갭률 +' || ROUND(v_latest_gap_rate * 100, 1) || '%)'
    ));
  END IF;

  -- ===== Score -> Policy mapping =====
  IF v_score <= -3 THEN
    v_policy := '마진상향';
    v_margin_adj := 1.5;
  ELSIF v_score <= -1 THEN
    v_policy := '소극적상향';
    v_margin_adj := 0.75;
  ELSIF v_score <= 1 THEN
    v_policy := '현상유지';
    v_margin_adj := 0;
  ELSIF v_score <= 3 THEN
    v_policy := '소극적인하';
    v_margin_adj := -1;
  ELSIF v_score <= 5 THEN
    v_policy := '점유율확대';
    v_margin_adj := -2;
  ELSE
    v_policy := '공격적인하';
    v_margin_adj := -3;
  END IF;

  RETURN QUERY SELECT v_score, v_policy, v_margin_adj, v_details;
END;
$function$;



================================================================================
# [파일 구분선] 아래부터 SQL 함수: 추천 판매가 산출 (price_recommend)
================================================================================


-- ============================================================
-- price_recommend: 추천 판매가 산출 함수
-- 기획서 섹션 2.1 + 2.3 수식 구현
-- 생성일: 2026-04-01
-- ============================================================

CREATE OR REPLACE FUNCTION price_recommend(
  p_product_code VARCHAR(6),
  p_date DATE,
  p_policy VARCHAR DEFAULT '현상유지',
  p_margin_adjustment NUMERIC DEFAULT NULL
)
RETURNS TABLE(
  base_purchase_price INT,
  current_selling_price INT,
  current_margin_rate NUMERIC,
  target_margin_rate NUMERIC,
  adjusted_margin_rate NUMERIC,
  recommended_sikbom_price INT,
  recommended_baemin_price INT,
  recommended_sinsun_price INT,
  recommended_oniljang_price INT,
  normal_price INT,
  recommendation_reason TEXT
)
LANGUAGE plpgsql AS $$
DECLARE
  v_base_purchase INT;
  v_current_selling INT;
  v_current_margin NUMERIC;
  v_target_margin NUMERIC;
  v_policy_adj NUMERIC;
  v_adjusted_margin NUMERIC;
  v_sikbom INT;
  v_sinsun INT;
  v_normal INT;
  v_is_event BOOLEAN;
  v_reason TEXT;
  v_emergency_price INT;
BEGIN
  -- ============================================================
  -- 1. 기준매입가 선정
  --    변곡점 가격이 있으면 신호해석층에서 p_margin_adjustment로 반영.
  --    여기서는 당일 매입가, 없으면 가장 최근 매입가를 사용.
  -- ============================================================
  SELECT purchase_price INTO v_base_purchase
  FROM daily_purchase_prices
  WHERE product_code = p_product_code AND price_date <= p_date
  ORDER BY price_date DESC LIMIT 1;

  IF v_base_purchase IS NULL THEN RETURN; END IF;

  -- ============================================================
  -- 2. 현재(전일) 판매가 조회
  -- ============================================================
  SELECT selling_price INTO v_current_selling
  FROM daily_selling_prices
  WHERE product_code = p_product_code AND price_date <= p_date
  ORDER BY price_date DESC LIMIT 1;

  -- 현재 수익률 = 1 - (매입가 / 판매가)
  IF v_current_selling IS NOT NULL AND v_current_selling > 0 THEN
    v_current_margin := ROUND((1.0 - v_base_purchase::NUMERIC / v_current_selling) * 100, 2);
  END IF;

  -- ============================================================
  -- 3. 목표수익률 결정
  --    products.target_margin_rate가 NULL이면 기본값 19.5%
  -- ============================================================
  SELECT COALESCE(p.target_margin_rate, 19.5), COALESCE(p.is_event_item, FALSE)
  INTO v_target_margin, v_is_event
  FROM products p WHERE p.product_code = p_product_code;

  IF v_target_margin IS NULL THEN v_target_margin := 19.5; END IF;

  -- ============================================================
  -- 4. 정책별 수익률 조정
  --    p_margin_adjustment가 주어지면 직접 사용, 아니면 정책명으로 매핑
  --    마진상향: +2%p, 소극적상향: +1%p, 현상유지: 0,
  --    소극적인하: -1%p, 점유율확대: -2%p, 공격적인하: -3%p
  -- ============================================================
  IF p_margin_adjustment IS NOT NULL THEN
    v_policy_adj := p_margin_adjustment;
  ELSE
    v_policy_adj := CASE p_policy
      WHEN '마진상향'   THEN  2.0
      WHEN '소극적상향' THEN  1.0
      WHEN '현상유지'   THEN  0.0
      WHEN '소극적인하' THEN -1.0
      WHEN '점유율확대' THEN -2.0
      WHEN '공격적인하' THEN -3.0
      ELSE 0.0
    END;
  END IF;

  -- 최소 수익률 하한선 10% 적용
  v_adjusted_margin := GREATEST(v_target_margin + v_policy_adj, 10.0);

  -- ============================================================
  -- 5. 식봄 추천판매가 = CEIL(매입가 / (1 - 수익률) / 10) * 10
  -- ============================================================
  v_sikbom := (CEIL(v_base_purchase / (1.0 - v_adjusted_margin / 100.0) / 10.0) * 10)::INT;
  v_reason := '정책: ' || p_policy || ' (수익률 조정 ' || v_policy_adj::TEXT || '%p)';

  -- ============================================================
  -- 6. 규칙 1: 긴급 일괄 조정
  --    현재 수익률이 19.5% 미만이면 수익률일괄변경가를 우선 적용
  --    일괄변경가 = CEIL(매입가 / (1 - 목표수익률) / 10) * 10
  -- ============================================================
  IF v_current_margin IS NOT NULL AND v_current_margin < 19.5 THEN
    v_emergency_price := (CEIL(v_base_purchase / (1.0 - v_target_margin / 100.0) / 10.0) * 10)::INT;
    IF v_emergency_price >= v_sikbom THEN
      v_sikbom := v_emergency_price;
      v_adjusted_margin := ROUND((1.0 - v_base_purchase::NUMERIC / v_sikbom) * 100, 2);
      v_reason := '긴급조정: 현재수익률 ' || v_current_margin::TEXT || '% < 19.5%, 일괄변경가 적용 (목표 ' || v_target_margin || '%)';
    END IF;
  END IF;

  -- ============================================================
  -- 7. 플랫폼별 판매가 연동
  --    배민 = 식봄 (동일)
  --    온일장 = 식봄 (동일)
  --    신선행 = MAX(CEIL(식봄*0.94/10)*10, CEIL(매입가/0.9/10)*10)
  -- ============================================================
  v_sinsun := GREATEST(
    (CEIL(v_sikbom * 0.94 / 10.0) * 10)::INT,
    (CEIL(v_base_purchase / 0.9 / 10.0) * 10)::INT
  );

  -- ============================================================
  -- 8. 행사품목: 정상가 = 판매가 * 1.25 (ROUNDUP to 10원)
  -- ============================================================
  IF v_is_event THEN
    v_normal := (CEIL(v_sikbom * 1.25 / 10.0) * 10)::INT;
  END IF;

  -- ============================================================
  -- 결과 반환
  -- ============================================================
  RETURN QUERY SELECT
    v_base_purchase, v_current_selling, v_current_margin,
    v_target_margin, v_adjusted_margin,
    v_sikbom,    -- 식봄
    v_sikbom,    -- 배민 = 식봄
    v_sinsun,    -- 신선행
    v_sikbom,    -- 온일장 = 식봄
    v_normal,    -- 정상가 (행사품목만, 비행사=NULL)
    v_reason;
END;
$$;



================================================================================
# [파일 구분선] 아래부터 백테스트 리포트
================================================================================


# 통합 백테스트 리포트: 엔진 추천가 vs 실제 판매가 (2025년)

**생성일**: 2026-04-01
**데이터 기간**: 2025-01-02 ~ 2025-12-31
**기본 목표수익률**: 19.5% (products 테이블에 target_margin_rate 미설정 → 기획서 기본값 적용)

---

## 데이터 현황

| 테이블 | 건수 |
|--------|------|
| products | 838 |
| daily_purchase_prices | 66,025 |
| daily_selling_prices | 60,578 |
| 매입+매출 조인 가능 건수 | 30,708 |
| 조인 가능 고유 상품 수 | 331 |

---

## 1. 실제 수익률 분포

### 1-1. 전체 퍼센타일

| 지표 | 값 |
|------|------|
| 총 건수 | 30,708 |
| 평균 수익률 | **23.01%** |
| 중앙값 | **22.22%** |
| 표준편차 | 15.24% |
| P10 | 11.89% |
| P25 | 19.03% |
| P75 | 28.93% |
| P90 | 37.80% |
| 최소 | -784.35% (이상치) |
| 최대 | 78.42% |

**수익률 19.5% 미만 비율**: 28.28% (8,684건 / 30,708건)
→ 약 3건 중 1건은 목표수익률 미달. 규칙1(수익률일괄변경) 적용 대상이 상당함.

### 1-2. 월별 평균 수익률

| 월 | 건수 | 평균 수익률 | 중앙값 |
|----|------|------------|--------|
| 2025-01 | 1,595 | 22.94% | 22.22% |
| 2025-02 | 2,033 | 23.57% | 22.31% |
| 2025-03 | 2,197 | **24.03%** | 23.33% |
| 2025-04 | 2,706 | 23.08% | 22.73% |
| 2025-05 | 2,455 | 23.32% | 22.22% |
| 2025-06 | 2,483 | 23.30% | 22.78% |
| 2025-07 | 3,011 | 23.59% | 22.85% |
| 2025-08 | 2,761 | **24.12%** | 23.00% |
| 2025-09 | 3,002 | 23.11% | 22.03% |
| 2025-10 | 2,497 | **21.15%** | 21.70% |
| 2025-11 | 2,773 | 23.54% | 22.05% |
| 2025-12 | 3,195 | **20.89%** | 21.35% |

**관찰**: 10월(21.15%)과 12월(20.89%)에 수익률이 유의미하게 하락. 계절적 매입가 상승(김장철 등) 대비 판매가 인상이 늦어진 것으로 추정.

---

## 2. 판매가 변경 패턴

| 지표 | 값 |
|------|------|
| 전일 대비 비교 가능 건수 | 59,906 |
| 변경 건수 | 28,095 |
| **일일 변경률** | **46.90%** |
| 인상 건수 | 13,931 |
| 인하 건수 | 14,164 |
| **인상 비율** (변경 중) | **49.59%** |
| **인하 비율** (변경 중) | **50.41%** |
| 평균 절대 변경폭 | 11.86% |
| 변경폭 중앙값 | 8.11% |

**관찰**: 인상과 인하가 거의 대칭적(50:50). 하루 기준 약 절반 가까이 가격이 변동되며, 변경폭 중앙값 8.11%는 상당히 큰 편 → 야채 시장의 높은 가격 변동성을 반영.

---

## 3. 기본 추천가 vs 실제가 괴리율 (규칙1: 목표수익률 19.5%)

**추천가 공식**: CEIL(매입가 / (1 - 0.195) / 10) * 10

### 3-1. 전체 통계

| 지표 | 값 |
|------|------|
| 비교 건수 | 30,708 |
| 평균 괴리율 | **-4.23%** (추천가가 실제가보다 낮은 경향) |
| 중앙값 괴리율 | -3.24% |
| 평균 절대 괴리율 | 11.26% |
| P10 괴리율 | -22.65% |
| P90 괴리율 | +9.53% |
| **+-3% 이내 비율** | **28.82%** |
| **+-5% 이내 비율** | **43.35%** |
| **+-10% 이내 비율** | **62.31%** |

### 3-2. 괴리율 분포 (히스토그램)

| 구간 | 건수 | 비율 |
|------|------|------|
| < -20% | 3,886 | 12.65% |
| -20% ~ -10% | 4,683 | 15.25% |
| -10% ~ -5% | 4,421 | 14.40% |
| -5% ~ -3% | 2,896 | 9.43% |
| **-3% ~ 0%** | **5,702** | **18.57%** |
| **0% ~ 3%** | **3,539** | **11.52%** |
| 3% ~ 5% | 1,215 | 3.96% |
| 5% ~ 10% | 1,396 | 4.55% |
| 10% ~ 20% | 1,405 | 4.58% |
| >= 20% | 1,565 | 5.10% |

**관찰**:
- 분포가 음의 방향으로 치우침 (추천가 < 실제가). 즉 형민님은 19.5%보다 높은 마진으로 판매하는 경우가 더 많음.
- 실제 평균 수익률이 23.01%이므로, 19.5% 기준 추천가는 당연히 실제가보다 낮게 나옴.
- 이는 **target_margin_rate를 상품별로 세분화하면 괴리율이 크게 개선될 것**을 시사.

---

## 4. 방향 일치율

매입가 전일 대비 변동 방향 vs 판매가 전일 대비 변동 방향:

| 지표 | 값 |
|------|------|
| 비교 가능 건수 | 30,409 |
| 매입가 변동 건수 | 15,394 (50.62%) |
| 판매가 변동 건수 | 14,631 (48.11%) |
| **방향 일치 건수** | **5,104** |
| 매입 변동 + 판매 미변동 | 4,745 |
| 방향 불일치 (반대 방향) | 5,545 |
| **엄격 방향 일치율** | **33.16%** |
| 일치 + 보류 비율 | 63.98% |

**관찰**:
- 엄격 방향 일치율 33.16%는 기준(80%) 대비 매우 낮음.
- 그러나 이는 "당일 즉시 반영" 기준이며, 형민님은 매입가 변동을 1~수일 후에 반영하는 패턴일 가능성이 높음.
- 매입가 변동 시 판매가를 변경하지 않은 비율(30.83%)도 상당 → 의도적 마진 흡수/방출 전략.
- **시차(lag)를 고려한 방향 일치율 분석이 S00-D에서 필요**.

---

## 5. 주요 경쟁 품목 vs 일반 품목

### 5-1. 수익률 비교

| 구분 | 건수 | 고유상품 | 평균수익률 | 중앙값 | 19.5%미만 비율 | 표준편차 |
|------|------|----------|-----------|--------|---------------|---------|
| **주요 경쟁 (28종)** | 5,712 | 28 | **24.20%** | 24.77% | 26.70% | **17.67%** |
| 일반 (303종) | 24,996 | 303 | 22.74% | 21.99% | 28.64% | 14.62% |

### 5-2. 변경 빈도 비교

| 구분 | 비교건수 | 변경건수 | 변경률 |
|------|----------|----------|--------|
| **주요 경쟁** | 12,247 | 7,666 | **62.59%** |
| 일반 | 47,659 | 20,429 | 42.86% |

### 5-3. 괴리율 비교

| 구분 | 건수 | 평균절대괴리율 | +-5%이내 | +-10%이내 |
|------|------|---------------|----------|----------|
| **주요 경쟁** | 5,712 | **13.28%** | 27.07% | 51.33% |
| 일반 | 24,996 | 10.80% | 47.07% | 64.82% |

### 5-4. 방향 일치율 비교

| 구분 | 매입변동건수 | 방향일치 | 일치율 |
|------|------------|----------|--------|
| **주요 경쟁** | 4,183 | 1,593 | **38.08%** |
| 일반 | 11,211 | 3,511 | 31.32% |

**관찰**:
- 주요 경쟁 품목은 평균 수익률이 더 높으나(24.20% vs 22.74%), 변동성도 더 큼(std 17.67%).
- 가격 변경 빈도가 20%p 높음(62.59% vs 42.86%) → 시장 가격에 더 민감하게 반응.
- 괴리율은 오히려 더 큼(13.28% vs 10.80%) → 주요 경쟁 품목에 대해 형민님은 19.5%와 다른 별도 마진 전략을 적용 중.
- 방향 일치율은 약간 더 높음(38.08% vs 31.32%) → 매입가 변동에 더 빠르게 반응.

---

## 6. 상품별 괴리율 순위

### 6-1. 괴리율 최대 상위 20 (튜닝 우선순위)

| 순위 | 상품코드 | 상품명 | 건수 | 평균괴리율 | 평균절대괴리율 |
|------|----------|--------|------|-----------|--------------|
| 1 | 004350 | 참나물 500g/국내산 | 197 | +25.91% | **40.64%** |
| 2 | 003235 | 피메추리알/국내산 | 34 | +32.25% | 36.35% |
| 3 | 003913 | 비타민 1kg/국내산 | 10 | +29.07% | 32.90% |
| 4 | 002934 | 쑥갓 250g/국내산 | 323 | -19.45% | 29.18% |
| 5 | 003270 | 봄동/국내산 | 56 | +4.18% | 26.48% |
| 6 | 003013 | 피망/청 500g/국내산 | 154 | -15.65% | 25.09% |
| 7 | 002795 | 적겨자/국내산 | 63 | +4.31% | 24.28% |
| 8 | 002805 | 홍고추 1kg/국내산 | 86 | -8.41% | 24.08% |
| 9 | 003544 | 돗나물/국내산 | 36 | +5.11% | 24.00% |
| 10 | 002792 | 홍갓(국내산) | 17 | -17.23% | 23.46% |
| 11 | 004652 | 피망/홍 500g/국내산 | 56 | -12.77% | 23.38% |
| 12 | 003924 | 피망/홍 1kg/국내산 | 37 | -8.30% | 23.26% |
| 13 | 003527 | 쑥갓 1kg/국내산 | 225 | -13.14% | 22.76% |
| 14 | 002898 | 영양부추/국내산 | 223 | -17.48% | 22.70% |
| 15 | 004566 | 상추A 500g/국내산 | 272 | -12.92% | 22.62% |
| 16 | 002998 | 케일/국내산 | 135 | -1.24% | 22.51% |
| 17 | 004026 | 청양고추/상 500g/국내산 | 323 | -21.04% | 22.47% |
| 18 | 004346 | 홍고추 500g/국내산 | 236 | -17.01% | 22.30% |
| 19 | 003231 | 쑥갓/국내산 | 107 | +3.87% | 21.99% |
| 20 | 004467 | 비타민/국내산 | 11 | -5.52% | 21.57% |

**패턴**: 엽채류(쑥갓, 상추, 케일, 부추)와 과채류(피망, 고추)가 괴리율 상위에 집중. 이 품목들은 가격 변동폭이 크고, 19.5% 고정 마진으로는 실제 가격 책정 패턴을 포착 불가.

### 6-2. 괴리율 최소 하위 20 (엔진이 잘 맞는 상품)

| 순위 | 상품코드 | 상품명 | 건수 | 평균절대괴리율 |
|------|----------|--------|------|--------------|
| 1 | 002195 | 4kg 땅콩자반/반찬단지/중국산 | 20 | **0.16%** |
| 2 | 003392 | 염장다시마 1kg/국내산 | 18 | 0.38% |
| 3 | 003506 | 청국장/덕소 4kg/중국산 | 38 | 0.68% |
| 4 | 003845 | 건취나물 1kg/중국산 | 16 | 0.78% |
| 5 | 005350 | 콩나물/두절/A-1/외국산 | 65 | 0.86% |
| ... | ... | ... | ... | ... |
| 20 | 002796 | 고구마/밤 1kg/국내산 | 33 | 1.79% |

**패턴**: 가공품(자반, 청국장), 건조품(건취나물, 다시마), 안정 가격 품목(콩나물, 고구마)이 잘 맞음. 매입가 변동이 작은 품목일수록 단순 마진 공식이 효과적.

---

## 7. 전체 요약 및 성공 기준 평가

### 성공 기준 충족 여부

| 기준 | 목표 | 실제 | 판정 |
|------|------|------|------|
| 방향 일치율 80% 이상 | >= 80% | **33.16%** | **미충족** |
| 가격 괴리 +-5% 이내 70% 이상 | >= 70% | **43.35%** | **미충족** |

### 미충족 원인 분석 및 개선 방향

**1. 방향 일치율 33.16% (목표 80%)**
- **근본 원인**: 당일(D+0) 기준 비교는 비현실적. 형민님은 매입가 변동을 D+1~D+3에 반영하는 패턴.
- **개선 방안**:
  - S00-D에서 시차(lag 1~5일) 적용 방향 일치율 재측정
  - 7일 이동평균 기반 추세 방향으로 비교 기준 변경
  - "매입가 변동 시 판매가 미변경(보류)"도 정상으로 포함하면 → 63.98%

**2. 가격 괴리 +-5% 이내 43.35% (목표 70%)**
- **근본 원인**: 전 상품에 일괄 19.5% 목표수익률 적용. 실제 평균 수익률은 23.01%.
- **개선 방안**:
  - target_margin_rate를 상품별/카테고리별로 세분화 (S00-A 데이터 보강 필요)
  - 실제 수익률 중앙값(22.22%) 기준 재계산 시 괴리율 대폭 감소 예상
  - 엽채류/과채류는 변동폭 자체가 20%+ → 고정 마진이 아닌 밴드(band) 방식 필요

### 핵심 발견사항

1. **실제 수익률 23.01%**: 형민님은 목표(19.5%)보다 평균 3.5%p 높은 마진으로 운영 중.
2. **계절성 존재**: 10월/12월에 수익률 하락 (각각 21.15%, 20.89%) → 계절 보정 필요.
3. **주요 경쟁 품목은 별도 전략 필요**: 변동성 크고(std 17.67%), 변경 빈도 높고(62.59%), 괴리율 큼(13.28%).
4. **가공/건조품은 엔진이 잘 맞음**: 괴리율 1% 미만 → 자동화 우선 대상.
5. **엽채류(쑥갓/상추/부추)와 과채류(피망/고추)는 튜닝 최우선 대상**: 괴리율 20%+.

### S00-D 튜닝 우선순위 제안

| 우선순위 | 작업 | 기대 효과 |
|----------|------|-----------|
| 1 | 상품별 실제 수익률로 target_margin_rate 역산 설정 | 괴리율 +-5% 이내 → 60%+ 예상 |
| 2 | 시차(D+1~D+3) 적용 방향 일치율 재측정 | 방향 일치율 50%+ 예상 |
| 3 | 7일 이동평균 기반 추세 판정 로직 적용 | 노이즈 제거, 안정적 추천 |
| 4 | 엽채류/과채류 전용 변동폭 밴드 설정 | 상위 20개 괴리율 15%→10% 개선 |
| 5 | 계절 보정 계수 도입 (10월/12월) | 월별 수익률 편차 축소 |

---

*이 리포트는 규칙1(수익률일괄변경가) 단독 적용 기준이며, 3층 의사결정 엔진(신호해석-전략선택-가격산출)의 풀 파이프라인 적용 시 성능이 크게 개선될 것으로 예상됩니다.*
