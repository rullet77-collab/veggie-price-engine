# 엔진 로직 명세 — 확정된 핵심 규칙

> **확정된 비즈니스 로직.** 형민님이 이미 결정한 것 — 다시 묻지 않는다.
> 작업 규칙은 `CLAUDE.md`, 단계별 확인은 `엔진_체크리스트.md` 참고.
> 로직 변경 시 이 문서를 먼저 갱신하고 코드를 고친다.

---

## 1. 데이터 흐름

```
/upload → /api/upload/rawdata
  ├─ 상품별매입현황 시트 → daily_purchase_prices
  │     → learn_tiers RPC + learnTierRatios + rollSellingPrices 자동 호출
  └─ 월별매출상세 (RAW 일자별 / 통합 채널별) → sales_detail (UPSERT)
        → recompute_monthly_sales RPC + rollSellingPrices 자동 호출

priceDate = daily_purchase_prices.max
```

### ★ mgmt(기존/변경) 완전 폐기 — 확정
- 천년경영 "기존(MMDD)/변경(MMDD)" 시트는 **업로드 자체를 안 한다**.
  rawdata route 의 파서·시트 감지·`daily_product_management` upsert path 모두 제거됨.
- 가격 엔진(추천가 `rollSellingPrices` / `/api/products` / 수익률일괄변경
  `bulk-apply-target`)은 **`daily_purchase_prices`(매입) + `sales_detail`(매출) raw 만** 사용.
  mgmt fallback 완전 제거.
- daily 매입 이력이 없는 상품 → 옛 mgmt 값 사용 안 함. 대신:
  - 박스소분 89개 → 박스↔소분 관계식 역산 (3절)
  - 일반 상품 → 그룹 reference 변동률 (2절 [3] / Layer 4-B)
  - 둘 다 불가 → 추천 불가 (ai_price=0)
- `daily_product_management` 테이블 자체는 DB 에 남아있음 (과거 데이터 보존). 읽지도 쓰지도 않음.

- **매출 raw**: UPSERT 정책 (같은 ROWKEY 재업로드 시 정정 반영). INSERT IGNORE 아님.
- **통합 매출파일**도 sales_detail 에 들어감 (파일명에 "매출"+채널 있으면 인식). 매입파일과 구분.
- 화면 `/products` 는 focus/visibilitychange/localStorage("products:invalidate") 시 자동 refetch.

---

## 2. 매입가 계산 우선순위 ★최우선 규칙★

### 2.0 같은 날 매입가 여러 건 → 대표가 선정 (전처리)
같은 (상품, 날짜) 에 매입가가 여러 건 들어올 수 있다 (매입처가 다르거나, 재고소분변경으로
다른 단가가 등록되는 경우). 이때 **거래수량(quantity) 이 가장 많은 가격을 그날 대표 매입가**로 쓴다.
동량이면 더 비싼 가격. (예: 005940 5/29 — 1,467원(27통) vs 2,700원(6통) → 1,467원)
- 적용처: `rollSellingPrices.ts`, `api/products/route.ts`, `api/dashboard/route.ts` 의
  매입 이력 집계 (7일동향·8일 basePP·60일 장기·등락 TOP10 전부 대표가 1건 기준).

**모든 매입가 계산의 1순위 판단은 "calc_group(가족) 태깅 여부" 이다.** (2026-07 개편)

```
[1] calc_group + relation_type = 소분관계(A형)/수량동일(B형)?
     → 가족 재산출 (3.2.1 박스경유, 8일 소급). 소분 실매입도 직접 안 씀.
       가족 전체 오늘 매입 없으면 → 같은 가족 등락률공유 대장 rate 차용 (방향2).

[2] relation_type = 등락률공유 (소그룹)?
     → 자기 실매입 있으면 그대로 basePP_v3.
       오늘 실매입 없으면 → 중그룹 가족재산출 오늘 rate 차용 (방향1).

[3] 미분류 + 매입 이력 충분(8일창 5일+)
     → 자기 매입 8일 이력으로 basePP_v3 (4절)

[4] 미분류 + 매입 이력 부족
     → 그룹 reference 변동률(ref_change)만.
```

### 2.2 등락률 차용 규칙 (2026-07 확정)
- **대장 선정**: 롤링 90일(priceDate−90일) distinct 매입일수 최다. 동점이면 product_code 오름차순.
- rate = (오늘 대표가 − 직전 대표가) ÷ 직전 대표가. **감쇠·단위계수 없이 순수 %**.
- 차용값 = ceil10(자기 마지막 대표가 × (1+rate)). **오늘만 채움** (과거 backfill 없음),
  daily_purchase_prices 에 되써넣지 않음. 7일동향 표기: inferred "등락률차용(대장코드)".
- 구현: `lib/purchaseHistory.ts applyRateBorrowing` (roll/products 공용).

### 2.3 ★토큰매칭 완전 제거 (2026-07 확정)
상품명 토큰 유사도 기반 단위환산(kg 비례 가격 복사)은 **매입가 계산·7일동향 화면 표시
모두에서 제거**. 잘못된 추정값 대신 빈칸(missing)이 정답 (예: 022485 양배추/수입산이
양배추/미니를 anchor 로 자릿수 다른 가격을 표시하던 버그의 근본 해결).
- 그룹 reference 변동률(ref_change, 이름 무관)은 유지.
- tokenizeName 등 함수는 화면 분류용으로만 잔존 (가격 계산 미사용).
- group_tier_ratios / learned_tier 는 용도 소멸 — 로드만 잔존, 정리 여부 별도 결정.

---

## 3. 박스소분 89개 상품 (박스 28 / 소분 61)

박스 하나를 소분 봉지로 쪼개 파는 상품군. `products.pack_role` ∈ {박스, 소분}, `pack_meta` JSONB.

### 3.1 상호보완 원칙
박스·소분은 **양방향** 환산. 박스 매입가만 있어도 소분 계산, 소분만 있어도 박스 계산.
**둘(박스+소분 전부) 다 매입 없을 때만** 관계식 불가 → 변동률 fallback.

### 3.2 공식 (10원 단위 올림 — 시스템 정책 ceil10)
```
박스 → 소분:  박스가 ÷ 공식수 × 소분수량
소분 → 박스:  소분가 ÷ 소분수량 × 공식수
소분 → 소분:  소분가 ÷ 소분수량 × 내수량  (단위가 거쳐 환산)
```
- `공식수` = 박스 pack_meta.formula_divisor
- `소분수량` = 소분 pack_meta.quantity (kg/개/봉/묶음/통). 250g→0.25, 500g→0.5 식으로 저장.
- 함수: `boxToSubdiv`, `subdivToBox` (aiRecommendation.ts)

### 3.2.1 ★신규 확정 — 2값 분리 (실제무게 + 공식수) 및 3단계 그룹 (2026-07 확정·엔진 반영 완료)
- 박스 pack_meta 는 **두 값**을 가진다: `actual_weight`(실제 물리량 — 소분→박스 원가 환산용, 마진 제거)
  + `formula_divisor`(공식수 — 박스→소분 재산출용, 소분마진 반영). 비대칭.
  예: 꽈리 박스 4kg — 소분→박스는 ×4, 박스→소분은 ÷3.5.
- **소분 실매입이 있어도 직접 쓰지 않는다**: 소분 실매입 → actual_weight 로 박스 원가 환산 →
  formula_divisor 로 각 소분 재산출 (8일 이력 전체에 소급 적용 — 확정).
  재산출가를 매입 이력(daily_purchase_prices)에 되써넣지 않는다 (원본 보존, 부풀림 방지).
- **3단계 그룹**: products.calc_group(가족번호 = DB대그룹×100+순번) + relation_type
  (소분관계=A형 / 수량동일=B형(대칭, 마진 0) / 등락률공유 / 제외).
  기존 product_group 은 대그룹(분류·화면용)으로 유지. 31가족/97상품 태깅 완료 (2026-07).
- 마스터 데이터: `자료/그룹재분류/신그룹매핑안.xlsx` (반영 스크립트: scripts/apply-newgroups.js)
- **엔진 반영 (lib/purchaseHistory.ts `buildFamilyNormalizedHistory`)**: 날짜별 박스 원가 =
  ①박스 실매입 ②없으면 소분 실매입(code 오름차순 첫 건)을 actual_weight 로 역산 ③둘 다 없으면 그 날 skip.
  전 멤버 = 박스 원가 기준 재산출 (박스 그대로 / 소분 ÷공식수×수량). 3경로(roll/products/dashboard) 공용.
  - basePP_v3: calc_group 상품은 8일/60일 이력 전체를 재산출 이력으로 교체해 입력 (완충 유지)
  - 7일동향 슬롯: 자기 실매입 있던 날 actual / 그 외 inferred("가족재산출"). 가격은 모두 재산출가.
    가족 전체 매입 없는 날은 missing (2차 reference 보강 미적용 — 등락률 차용 단계에서 처리 예정)
  - 대시보드 등락: calc_group 상품은 실매입 등락 대신 가족재산출 등락으로 표시 (엔진 pp 와 일치)

### 3.3 공식수 (그룹별, GROUP_FORMULAS — tag-pack-formula.ts)
> ★값 정정 (2026-07 확정): 꽈리(34) 9 → **실제무게 4 / 공식수 3.5**.
> 깻잎(19) 10 → **100** (10은 묶음당 장수였음 — 1박스 = 10장짜리 100묶음). 무(21) 9 확정.
> 이하 표는 구 단일값 기준 — 최신 2값은 신그룹매핑안.xlsx / products.pack_meta 참조.
| 그룹 | 품목 | 공식수 | 단위 |
|------|------|--------|------|
| 1 | 가지 | 30 (시즌: 겨울11~6월 ÷30 / 여름7~10월 ÷45) | 개 |
| 3 | 감자 | 18 | kg |
| 4 | 양상추 | 12 | 개 |
| 5 | 양배추 | 3 | 통 |
| 6 | 양파 | 13 | kg |
| 7 | 배추 | 3 | 통 |
| 9 | 알배기 | 12 | 개 |
| 12 | 팽이버섯 | 34 | 봉 |
| 19 | 깻잎 | 10 | 묶음 |
| 21 | 무 | 9 | kg |
| 23 | 상추A | 3.5 | kg |
| 27·28·30·31·32·33·34·35·58 | 고추·피망류 | 9 | kg |
| 46 | 백오이 | 48 | 개 |
| 47 | 청오이 | 42 | 개 |
| 48·49·50 | 애호박·주키니 | 20 | 개 |
| 51 | 치커리 | 2 | kg |
| 64 | 쑥갓 | 3.5 | kg |
| 82 | 브로컬리 | 27 | 개 |
| 96 | 단호박 | 6 | 개 |

### 3.4 공식수의 의미
공식수는 박스 표기 무게와 다를 수 있다 (예: 양파 박스 15kg → 공식수 13, 감자 20kg → 18).
이유 = **소분마진** — 박스를 소분으로 쪼개 팔 때 로스/다듬기/마진을 공식수에 미리 반영.
공식수는 형민님이 `박스소분상품260420.xlsx` 로 확정한 값. 오타 아님. 그대로 쓴다.

---

## 4. basePP_v3 — 적정 매입가 산출 (박스소분 아닌 일반 상품)

8일 매입 이력으로 "적정 매입가" 산출. 일시적 급변에 추천가가 휘둘리지 않게 완충.

- 추세: 최근4일 평균 vs 과거 평균 → ±5% 기준 상승/하락/횡보
- 당일충격: ±15% 급등/급락
- 최빈값(3회+), 가중평균(최근0.7/과거0.3)
- 추세별 비대칭 선택 (상승: max / 하락: 완충 max / 횡보: median)
- **안정 신호**: 최근 2일 동가(sp[0]===sp[1]) + 하락 추세 → basePP = median(최빈, 오늘가)
  점진 수렴. 전체 상품 공통 적용 (calcBasePP_v3 함수).

---

## 5. 추천 분기 (calculateAiRecommendation)

target_margin = `products.target_margin_rate` (플랫폼시트 AR 컬럼, 기본수익률).
모든 분기의 기준점. 매입추세/매출/품목특성에 ±조정.

| 분기 | 조건 | 적용 마진 |
|------|------|----------|
| (A) 역마진 | cur ≤ basePP | targetPrice |
| (A-1) 상승이상치 | isAbnormal & 매입 급등 | max(FLOOR, target − 0.25×riseRate) |
| (A-2) 하락이상치 | **비활성** (prev_selling stale 위험) | — |
| (B) 큰폭상승 | totalTrend>5% & 상승 | target + 1~2 |
| (C) 완만상승 | totalTrend>2% | target + 1 |
| (D) 큰폭하락 | totalTrend<−5% & 하락 | target + 1~2 |
| (E) 완만하락 | totalTrend<−2% | target + 1 |
| (F)(G) 변곡·횡보 | else | target |

매출 보정(Phase3): 매출없음/급감/감소 → 인하, 호조 → 예민품목만 +0.5%p.
민감도 하한(FLOOR): 예민 18% / 고정·일반 15%.

---

## 6. products 컬럼 사전

| 컬럼 | 의미 |
|------|------|
| `target_margin_rate` | 기본수익률 (플랫폼시트 AR). 추천 분기 기준점 |
| `is_key_item` | 주요 경쟁품목 → 추천 마진 −1%p |
| `price_sensitivity` | 예민/고정/일반 → 마진 하한·매출보정 분기 |
| `pack_role` / `pack_meta` | 박스소분 (박스/소분 + 공식수·실제무게·수량). 97개 태깅 |
| `calc_group` | 계산그룹(가족) 번호 — DB대그룹×100+순번 (예: 감자 왕특=301). 같은 가족끼리만 박스↔소분 환산 |
| `relation_type` | 소분관계(A형·비대칭) / 수량동일(B형·대칭 마진0) / 등락률공유 / 제외. NULL=미분류 |
| `learned_tier` | 365일 매입가 NTILE(3). 1=top 2=mid 3=low. 그룹 anchor 정렬용 |
| `product_group` | 그룹 참조 / tier ratio |
| `purchase_source` | 매입처 풀. 같은 그룹이라도 다른 source 끼리는 직접 매입가 환산 X (변동률만). 예: 상추 B풀 004645/011211/012126 |
| `platform_status` | 판매중 / 판매중지. NULL=판매중. 판매중지 = 화면 기본 숨김 |
| `price_fixed` | 판매가고정 → rollSellingPrices·추천 자동산출 skip, selling_price 를 추천가로 |
| `product_type` | 야채(607) / 공산(231) |

---

## 7. selling_price 라이프사이클

- `selling_price` NULL = 추천가 자동 적용 모드 (사용자 미입력)
- `selling_price` 값 있음 = 사용자 수동 입력가 (보존)
- `prev_selling_price` = 어제 최종가 = COALESCE(어제 selling, 어제 recommended)
- `recommended_price` = rollSellingPrices 가 매입/매출 업로드 직후 일괄 산출
- `price_fixed=true` 상품 → recommended_price = selling_price 그대로 (자동산출 skip)

---

## 8. 학습 시스템 (매입 raw 업로드 시 자동)

- `learn_tiers` RPC — 365일 매입가 그룹·unit 내 NTILE(3) → products.learned_tier
- `learnTierRatios` — 그룹 내 등급(tier) 페어별 가격비 학습 → group_tier_ratios (B-3)
- `recompute_monthly_sales` RPC — sales_detail.channel 기반 monthly_sales_quantity 재집계 (매출 업로드 시)
