# 엔진 작업 체크리스트

> 작업 전 / 수정 중 / 커밋 전 — 각 단계에서 반드시 확인한다.

---

## ✅ 작업 전

- [ ] `CLAUDE.md` 확인 (작업 규칙)
- [ ] `엔진_로직_명세.md` 확인 (확정 로직)
- [ ] 수정 대상 함수 **전체** 읽기 (일부만 보고 수정 금지)
- [ ] 기존 로직을 한 문단으로 요약한 뒤 작업 시작
- [ ] "매입가 계산" 관련이면 → **박스소분 89개 해당 여부 먼저 판단**

## 🔒 수정 중 — 변경 금지 항목 (사용자 명시 승인 없이는)

- [ ] 박스소분 89개 공식 (박스↔소분 환산 / 공식수) 변경 금지
- [ ] 매입가 계산 우선순위 (박스소분 → basePP_v3 → 토큰매칭) 변경 금지
- [ ] selling_price 라이프사이클 (NULL=자동 / prev 롤오버 / price_fixed) 변경 금지
- [ ] basePP_v3 분기 (추세/충격/안정신호) 삭제 금지
- [ ] 기존 fallback 로직 삭제 금지 (변동률 reference, 토큰매칭 등)
- [ ] (A-2) 하락이상치 비활성 상태 유지

## 🔎 수정 중 — 정확성 확인

- [ ] 박스소분 상품에 토큰매칭 쓰지 않았는가
- [ ] 단위환산 시 spec 의 kg/g 차이 반영했는가 (같은 unit 이라도 spec 다르면 비례)
- [ ] priceDate = max(mgmt.max, daily.max) 로 잡았는가
- [ ] 금액 10원 단위 올림(ceil10) 적용했는가
- [ ] 화면(브라우저) stale 값으로 분석하지 않았는가 — API/SQL 로 검증했는가

## 📦 커밋 전

- [ ] 명세서(`엔진_로직_명세.md`)와 코드 불일치 여부 확인
- [ ] 로직을 변경했으면 → `엔진_로직_명세.md` 갱신 (문서-코드 동기화)
- [ ] API/SQL 로 검증한 결과를 기록했는가
- [ ] 로직을 삭제했으면 → 삭제 사유 명시
- [ ] 커밋 범위 = 작업 단위 (무관한 파일 섞지 않기)
- [ ] dev 브랜치, 사용자가 "커밋" 지시했을 때만
