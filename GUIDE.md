# 📑 프로젝트 관리 및 운영 가이드라인

본 문서는 업무 효율화와 안정적인 자동화 시스템 구축을 위한 코드 관리 및 운영 규정을 정의합니다. 모든 작업자(AI 및 개발자)는 본 내용을 숙지하고 준수해야 합니다.

---

## 1. 소스코드 관리 전략 (Git Strategy)

### 1.1 브랜치 구조 및 역할
* **main**: 제품의 완성본이 저장되는 브랜치입니다.
    * 항상 오류가 없고 실행 가능한 상태를 유지해야 합니다.
    * `dev` 브랜치에서 기능 검증 및 버그 테스트가 완료된 항목만 머지(Merge)합니다.
* **dev**: 실제 개발 및 단위 테스트가 진행되는 브랜치입니다.
    * 모든 개발 작업은 이 브랜치에서 시작하며, 작업 시 반드시 이 브랜치로 전환 후 진행합니다.

### 1.2 커밋 및 푸시 (Commit & Push)
* **최소 단위 커밋**: 하나의 커밋에는 '하나의 기능' 혹은 '하나의 수정 사항'만 포함합니다. (아주 작은 단위 지향)
* **상시 푸시**: 로컬 작업이 완료되면 즉시 원격 저장소(`dev` 브랜치)에 푸시하여 코드 유실을 방지합니다.
* **머지 조건**: `dev` 브랜치에서 모든 기능이 정상 동작하고 버그가 없음을 확인한 후에만 `main`으로 머지합니다.

## 2. 코드 수정 및 보호 원칙
* **임의 수정 금지**: 기존에 정상 동작하는 코드를 사용자의 명확한 승인 없이 임의로 수정하거나 삭제하지 않습니다.
* **제안 후 실행**: 코드 개선이나 리팩토링이 필요한 경우, 먼저 장단점과 개선 가능성을 제시하고 사용자가 허락한 부분에 대해서만 코드를 수정합니다.
* **주석 관리**: 불필요하거나 반복적인 주석을 추가하여 코드의 가독성을 해치지 않습니다.

## 3. 자산 및 보안 관리
* **대용량 파일 관리**: 깃허브 레포지토리에는 코드만 관리합니다.
    * 데이터셋(.csv, .json, .xlsx 등), 학습된 AI 모델, 로그 파일 등은 깃허브에 업로드하지 않습니다.
* **.gitignore 설정**: 보안이 필요한 API Key나 대용량 파일은 반드시 `.gitignore`에 등록하여 관리합니다.
* **백업 경로**: 깃허브에 올라가지 않는 데이터 및 모델은 별도로 지정된 클라우드(Google Drive, AWS 등) 백업 위치에 업로드하여 관리합니다.

---
*본 문서는 프로젝트 진행 상황에 따라 업데이트될 수 있습니다.*